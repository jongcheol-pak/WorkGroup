<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="WorkGroup.App.Views.MainShell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    mc:Ignorable="d">

    <!--
        앱 셸 — WinUI Gallery 표준(plan.md DG3/DG6).
        - 1행: TitleBar(앱 아이콘 + 제목). 창의 ExtendsContentIntoTitleBar=true와 자동 동기(App.xaml.cs).
        - 2행: NavigationView(Left, 280 pane). 컨텐츠 마진/보더 제거로 사이드바와 본문 직접 인접(설정 화면 패턴).
    -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar Grid.Row="0" x:Name="AppTitleBar" Title="WorkGroup">
            <!-- IconSource는 크기가 고정(약 16px)이라, 크게 표시하기 위해 LeftHeader에 직접 Image를 배치한다. -->
            <!-- 배치 순서상 LeftHeader는 제목 왼쪽에 온다. -->
            <TitleBar.LeftHeader>
                <Image Source="ms-appx:///Assets/AppIcon.ico"
                       Width="28" Height="28"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0" />
            </TitleBar.LeftHeader>
        </TitleBar>

        <NavigationView
            Grid.Row="1"
            x:Name="Nav"
            Background="Transparent"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False"
            IsTitleBarAutoPaddingEnabled="False"
            OpenPaneLength="{StaticResource SideNavWidth}"
            PaneDisplayMode="Left"
            Loaded="OnLoaded"
            SelectionChanged="OnSelectionChanged">

            <NavigationView.Resources>
                <!-- 컨텐츠 영역 마진/보더 제거 → 사이드바 바로 옆에서 본문 시작 -->
                <Thickness x:Key="NavigationViewContentMargin">0</Thickness>
                <Thickness x:Key="NavigationViewMinimalContentGridBorderThickness">0</Thickness>
                <CornerRadius x:Key="NavigationViewContentGridCornerRadius">0</CornerRadius>
                <Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>
            </NavigationView.Resources>

            <NavigationView.MenuItems>
                <NavigationViewItem Content="작업 그룹" Tag="WorkGroups">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="AllApps" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="트레이 메뉴" Tag="TrayMenu">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="List" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="설정" Tag="Settings">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Setting" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="정보" Tag="About">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Help" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" Background="Transparent" />
        </NavigationView>
    </Grid>
</Page>
