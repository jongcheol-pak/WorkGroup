<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="WorkGroup.App.Views.TrayMenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="{StaticResource PageContentPadding}" HorizontalScrollMode="Disabled">
        <StackPanel MaxWidth="{StaticResource ContentMaxWidth}" HorizontalAlignment="Stretch" Spacing="4">
            <StackPanel Spacing="4" Margin="0,0,0,8">
                <TextBlock Text="트레이 메뉴" Style="{ThemeResource TitleTextBlockStyle}" />
                <TextBlock Text="알림 영역(트레이) 관련 설정입니다."
                           Style="{ThemeResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <InfoBar IsOpen="True" IsClosable="False" Severity="Informational"
                     Title="추후 추가 예정"
                     Message="트레이 메뉴 설정 기능은 작업 그룹 기능 완료 후 추가될 예정입니다." />
        </StackPanel>
    </ScrollViewer>
</Page>
