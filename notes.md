# 작업 노트

## 최근 변경
- 2026-06-03: 폴더 팝업 닫힘/포커스 메커니즘 재설계(AppGroup 방식 이식, GUI 검증 완료) — 기존 _isActive/마우스 위치(GetCursorPos) 기반 닫힘이 누적·다른 앱 클릭 시 안 닫힘 등 반복 버그를 유발해 전면 교체. AppGroup의 핵심 패턴: ① **자식(2차+) 팝업은 포커스를 뺏지 않고 표시**(AppWindow.Show(activateWindow:false), child.Activate() 제거) → 1차가 활성 상태 유지. ② **1차만 SetForegroundWindow로 포그라운드 포커스 확보**(트레이 클릭 직후 Activate()만으론 부족) → 다른 앱/작업표시줄/바탕화면 클릭 시 1차 Deactivated → CloseSelf → OnClosed가 자식 체인 닫음(전체 닫힘). ③ child.Closed는 ReferenceEquals(_child, child)로 추적만 해제(자식 교체 시 늦게 온 Closed가 새 _child를 null로 덮어쓰던 누적 버그 차단). ④ 폴더 클릭=탐색기 열기+체인 닫기, 하위 탐색은 호버로. ⑤ 마우스 위치 판정(IsPointerOverChain/Window/GetCursorPos)·_isActive·_childOpen 전부 제거(복잡도↓). FolderListPopupWindow/FolderContentsPopupWindow 공통. 빌드 0/0.
- 2026-06-03: 폴더 팝업 후속 수정(GUI 검증 완료) — ① 팝업 가로 폭 확대(1차 FolderListPopupWindow.SingleColumnWidth 360, 2차 FolderContentsPopupWindow.PopupWidth 400). ② 항목 이름 말줄임: 가로 StackPanel은 자식을 무한 너비로 측정해 TextTrimming(CharacterEllipsis)이 동작 안 함 → 항목 버튼 콘텐츠를 Grid(아이콘 Auto + 이름 *)로 변경해 긴 이름이 "..."로 표시. ③ 닫힌 창 접근 COMException(0x800710DD) 근본 차단: 모든 닫기 경로(자가/부모→자식/App→1차 _folderPopup)를 CloseSelf로 통일해 Close() 전 _closed를 동기 set(Closed 이벤트는 큐에서 Tick보다 늦게 와 가드 누설), Content/AppWindow에 접근하는 모든 비동기 진입점(AdjustToContent[SizeChanged], RevealAtTaskbar/RevealAtAnchor[DispatcherQueue], OnHoverTick[타이머])에 _closed 가드 + OnHoverTick COMException try-catch 안전망. ④ 2차→1차 마우스 복귀 시 전체가 닫히던 문제: 창 활성(_isActive)은 마우스 hover로 안 바뀌므로 부적합 → child.Closed 후 GetCursorPos 커서 좌표가 자기 창 사각형 위인지로 판정(복귀 시 유지, 밖이면 체인 종료, _child 교체 시 유지). 빌드 0/0.
- 2026-06-03: 폴더 바로가기(트레이 좌클릭 폴더 팝업) 기능 신규 추가(plan T1~T12) — AppGroup의 "시작 메뉴 폴더" 기능 이식, 기존 "작업 그룹"과 별개. ① Domain: FolderShortcut(Id/Name/Path, Create 검증)·FolderPopupSettings(열/깊이 1~5 클램프·Default(1,2,false)). ② Application: IFolderShortcutRepository·IDirectoryBrowser(DirectoryListing/Status)·IShellOpener. ③ Infrastructure: JsonFolderShortcutRepository(folders.json, JsonGroupRepository 패턴=원자적 쓰기/손상 백업/SemaphoreSlim, Id 단조증가·경로 중복검사)·DirectoryBrowser(숨김 필터/이름 정렬/상태)·ShellOpener(Process.Start ShellExecute)·ShellIcon.OpenForPathAsync(경로 아이콘 public 오버로드 추가, 기존 OpenForAppAsync 무변경)·WorkGroupPaths.FoldersConfigPath. ④ App: FolderPopupSettingsService(LocalSettings, ThemeService 패턴)·FolderIconLoader(경로→ImageSource)·FolderShortcutsViewModel/Item(검색 필터)·TrayMenuPage(placeholder→폴더 관리: 안내/검색/카운트/추가·설정/카드 목록/위치열기·삭제)·FolderEditDialog(이름+FolderPicker, 검증·저장)·FolderListPopupWindow(좌클릭 1차 목록, ColumnCount 1=세로/2~5=그리드, 톱니→트레이메뉴 탭)·FolderContentsPopupWindow(2차 파일/하위폴더, SubfolderDepth 재귀, 부모 좌/우 배치)·FolderPopupSettingsDialog. ⑤ 트레이 동작 변경: 좌클릭=폴더 팝업(TrayIconService.LeftClickRequested, WM_LBUTTONUP만·DBLCLK 상수 제거), 메인 창은 우클릭 메뉴 "열기"만(OpenRequested 유지). App.ShowFolderListPopup/ShowTrayMenuFromPopup, MainShell.SelectTrayMenu. ⑥ B2 포커스 가드: 부모-자식 팝업 체인(_childOpen/_isActive/_child/CloseChainRequested)으로 2차 팝업 표시 중 1차 유지, 파일 실행·최대깊이 폴더 열기 시 체인 전체 닫기, ChildPopupAnchor로 배치. ⑦ ServiceConfiguration DI 등록(저장소/브라우저/오프너/설정서비스/뷰모델). 빌드 0/0, 테스트 106/106(Domain 23: 신규 FolderShortcut 4+FolderPopupSettings 4, Application 83: 신규 JsonFolderShortcutRepository 8+DirectoryBrowser 4). DI 런타임 해석·트레이 좌/우클릭·팝업 표시/위치/호버/2차/포커스 체인·설정 반영은 F5 MSIX GUI 수동 검증.
- 2026-06-03: 트레이 아이콘을 앱 아이콘(AppIcon.ico)으로 적용 — TrayIconService.Initialize에서 LoadIcon(IDI_APPLICATION) 기본 아이콘 대신 LoadTrayIcon()으로 Assets\AppIcon.ico를 LoadImage(IMAGE_ICON, 작은 아이콘 크기 SM_CXSMICON/CYSMICON, LR_LOADFROMFILE) 로드. 실패 시 시스템 기본 아이콘 폴백. 파일에서 로드한 핸들은 소유(_ownedIcon)로 Dispose에서 DestroyIcon 해제. 경로는 AppContext.BaseDirectory 기준. 빌드 0/0. (트레이 표시는 F5 GUI 수동 검증.)
- 2026-06-03: 핀 팝업 항목 마우스 오버 배경 숨김 — GridView.Resources에서 GridViewItemBackgroundPointerOver/Pressed를 Transparent로 재정의해 항목 hover/누름 시 기본 강조 배경이 보이지 않게 함(아이콘 호버 확대 애니메이션만 유지). 빌드 0/0.
- 2026-06-03: 핀 팝업 표시 시 크기 점프·초기 깜빡임 제거 — 원인: 생성자에서 InitialPopupHeight(200)·정위치로 배치 후 Activate로 즉시 표시 → 비동기 LoadAsync/AdjustToContent가 실제 높이로 리사이즈하는 과정이 화면에 보임(점프), 콘텐츠/아이콘 로드 전 빈 창이 먼저 보임(깜빡임). 수정: ① 생성자에서 클릭 시점 커서/모니터(_metrics) 1회 캡처 후 창을 화면 밖(OffScreen -32000)으로 이동 — 측정/리사이즈를 화면 밖에서 끝냄. ② LoadAsync에서 아이콘(LoadIconAsync)까지 Task.WhenAll로 대기(실패 무시). ③ finally에서 RevealAtTaskbar로 최종 측정→정위치 이동(_positioned=true)해 처음으로 표시. PositionNearTaskbar(Resize+Move)를 AdjustToContent의 Resize와 MoveToTaskbar(Move, _metrics 사용)로 분리, 표시 전에는 Move 생략. 빌드 0/0. (점프·깜빡임 제거는 F5 GUI 수동 검증.)
- 2026-06-03: 핀 팝업 아이콘 호버 확대 애니메이션 — GridView ItemTemplate StackPanel에 중앙 기준 ScaleTransform + PointerEntered/Exited 이벤트. OnIconPointerEntered/Exited에서 AnimateIconScale(Storyboard, ScaleX/Y DoubleAnimation, 120ms CubicEase EaseOut)로 1.0↔1.15 부드럽게 전환. 빌드 0/0. (호버 효과는 F5 GUI 수동 검증.)
- 2026-06-03: 핀 클릭 시 팝업 창이 작업 표시줄/Alt+Tab에 노출되던 것 차단 — GroupPopupWindow.ConfigurePresenter에서 AppWindow.IsShownInSwitchers=false 설정. 핀 클릭으로 뜬 팝업 프로세스 창이 작업 표시줄 버튼/스위처에 별도 앱 아이콘·미리보기로 보이던 문제 제거(IsShownInSwitchers는 OverlappedPresenter가 아니라 AppWindow 속성). 빌드 0/0. (노출 차단은 F5 GUI 수동 검증.)
- 2026-06-03: 그룹 추가/수정 화면 개선(plan T1~T5) — ① 선택 앱 목록 위 왼쪽에 앱 개수 표시("앱 N개", GroupEditViewModel.AppCountText, SelectedApps.CollectionChanged 생성자 1회 구독으로 갱신). ② "앱 추가" 버튼을 "+" 아이콘만(SymbolIcon Add + ToolTip "앱 추가"). ③ 앱 추가 버튼 왼쪽에 "팝업 이름 표시" 토글(ToggleSwitch) — **그룹별 설정**. AppGroup에 ShowPopupHeader 속성 추가(Create/Restore 선택적 매개변수 기본 true, SetShowPopupHeader), JsonGroupRepository.GroupDto에 bool? ShowPopupHeader 추가(키 고정·레거시 JSON은 ?? true 마이그레이션·SchemaVersion 불변), GroupPopupWindow.LoadAsync가 group.ShowPopupHeader로 헤더 TitleText 표시/숨김(에러 메시지는 항상 표시, RowSpacing 제거+TitleText Margin으로 헤더 숨김 시 잔여 여백 없음). ④ 선택 앱 목록 항목을 Border로 감싸 하단 1px 구분선(DividerStrokeColorDefaultBrush)으로 행 구분. 도메인 호출자(테스트 다수)는 default라 무변경. 빌드 0/0, 테스트 86/86(Domain 15 신규4 + Application 71 신규2). (토글 저장→팝업 반영·카운트 갱신·구분선·헤더 숨김 여백은 F5 GUI 수동 검증.)
- 2026-06-03: 핀 그룹 팝업 아이콘 아래 앱 이름 제거 — DataTemplate의 이름 TextBlock(DisplayName) 삭제, 아이콘 타일만 표시. 콘텐츠 높이 자동 맞춤으로 팝업 세로도 함께 축소. PopupAppItem.DisplayName 속성은 유지. 빌드 0/0.
- 2026-06-03: 핀 그룹 팝업 흰 테두리 → 메인 창 스타일로 재구성 — borderless(SetBorderAndTitleBar(false,false)) 창에 Windows가 그리는 흰 윤곽선은 DWM(COLOR_NONE)으로도 제거 불가라, 메인 창과 동일한 표준 창 프레임 방식으로 전환. ① SetBorderAndTitleBar(true,false): 둥근 모서리/시스템 테두리는 유지하고 타이틀바(캡션 버튼)만 숨김. ② SystemBackdrop=MicaBackdrop()(메인 창과 동일 재질). ③ XAML 외곽 Border(아크릴 배경·CornerRadius·테두리)를 제거하고 루트를 Grid로 — 배경/모서리는 창 프레임이 담당. 앞선 DWM P/Invoke(상수·DllImport·RemoveSystemBorder)·관련 using 제거. 세로 높이 콘텐츠 맞춤(AdjustToContent)은 유지하되, 표준 프레임 전환으로 비클라이언트(테두리/캡션) 영역이 생겨 콘텐츠가 잘리던 것을 보정 — 측정한 클라이언트 높이에 chrome(AppWindow.Size.Height − ClientSize.Height)을 더해 창 높이를 정한다. 또한 ExtendsContentIntoTitleBar=true로 콘텐츠를 타이틀바 영역까지 확장해 상단 빈 캡션 여백 제거(타이틀바는 SetBorderAndTitleBar(true,false)로 숨김). 추가로 GridView가 내부 세로 스크롤(가상화)로 필요 높이를 줄여 보고해 콘텐츠 측정이 부정확→세로 잘림 발생하던 문제를, GridView ScrollViewer.VerticalScrollMode/VerticalScrollBarVisibility=Disabled로 전체 콘텐츠를 펼쳐 측정 정확화. 그래도 DispatcherQueue 1회 측정은 GridView 아이템 컨테이너 생성 전에 실행돼(콘텐츠 높이 과소→창 고정→라벨 잘림) 부정확 → 루트 SizeChanged를 구독해 컨테이너 생성·배치로 콘텐츠가 커진 시점에 재측정·재조정하고, _lastAppliedHeight 가드로 동일 높이 재적용(무한 루프) 차단. 빌드 0/0. (외곽 표시·잘림·여백은 GUI 수동 검증.)
- 2026-06-03: 핀 그룹 팝업(GroupPopupWindow) 테두리 제거 + 세로 높이 콘텐츠 맞춤 — ① 루트 Border의 BorderBrush/BorderThickness("1") 제거(둥근 모서리 CornerRadius 8·배경은 유지)로 회색 테두리선 삭제. ② 고정 높이 PopupHeight(440)를 폐기하고 InitialPopupHeight(200)로 초기 배치 후, 아이템 로드 완료 시(LoadAsync finally→DispatcherQueue) AdjustToContent에서 루트 콘텐츠를 측정(UpdateLayout+Measure, 너비 고정·높이 무한 제약)해 DesiredSize×RasterizationScale로 창 높이를 줄이고 작업 표시줄 근처로 재배치. PositionNearTaskbar(int height)로 시그니처 변경. 아이콘 수에 맞춰 세로 여백 제거. 빌드 0/0. (실제 팝업 크기·표시는 GUI 수동 검증.)
- 2026-06-03: 앱 아이콘(AppIcon.ico) 일괄 적용 — ① 실행 파일(.exe) 아이콘(csproj `ApplicationIcon`) ② 메인 창 작업 표시줄/Alt+Tab 미리보기 아이콘(App.xaml.cs `AppWindow.SetIcon(@"Assets\AppIcon.ico")`, 창 생성 블록 내 1회) ③ 타이틀바 아이콘을 AppIcon.ico로 변경 + 28px(IconSource는 16px 고정이라 `TitleBar.LeftHeader`에 Image 배치, 제목 왼쪽) ④ 정보 화면 앱 이름 카드 아이콘 AppIcon.ico + 48px(`SettingsCard.HeaderIcon`). AppIcon.ico를 루트→`Assets/`로 복사 후 csproj `Content` 포함(SetIcon/ms-appx 참조용). 기존 Square44x44Logo 타일 자산은 미변경(타일/스토어 로고 유지). 빌드 0/0, 테스트 80/80. (아이콘 시각 표시는 F5 MSIX GUI 수동 검증.)
- 2026-06-03: 그룹 수정 화면 이름 클릭-편집 + 핀 재등록 경고 — 수정 모드에서 그룹 이름을 읽기전용으로 표시하다 클릭하면 입력창(TextBox)으로 전환(GroupEditViewModel.IsNameEditing/NameDisplayVisibility/NameEditVisibility, code-behind에서 DispatcherQueue로 포커스+SelectAll). 이름이 원래와 달라지면(ShowRenameWarning) 입력 영역 아래 경고 InfoBar 표시("작업 표시줄에 핀한 기존 항목을 제거하고 다시 핀해야 합니다"). 핀 표시 이름은 Windows 제약상 프로그래밍 방식 자동 변경 불가라 안내만 함. 신규 추가는 기존대로 즉시 입력·경고 없음. GroupEditDialog.xaml Grid 4→5행 재구성. 빌드 0/0, 테스트 80/80. (이름 전환·포커스 등 UI 동작은 GUI 수동 검증.)
- 2026-06-02: 드래그 비주얼 빈 이미지 수정 — 화면 Image에 쓰이는 라이브 BitmapImage를 SetContentFromBitmapImage에 넘기면 드래그 표면에 빈 이미지로 렌더되는 문제. 아이콘 파일(PNG 우선, 없으면 .ico)을 SoftwareBitmap(BGRA8)으로 새로 로드해 SetContentFromSoftwareBitmap으로 지정(DragStarting deferral로 비동기 로드 대기). 드래그 비주얼 크기는 최대 128px(종횡비 보존·업스케일 금지). 드래그/핀 동작 정상, GUI 검증 완료. 빌드 0/0, 테스트 80/80.
- 2026-06-02: 그룹 목록 드래그 비주얼=그룹 아이콘 — 작업 표시줄로 드래그 시 커서 이미지가 항목 카드 스냅샷이 아니라 그룹 아이콘으로 표시. ListView CanDragItems/DragItemsStarting → 카드 Border CanDrag="True"+DragStarting로 전환, DragStartingEventArgs.DragUI.SetContentFromBitmapImage(item.Icon) 사용. 기존 .lnk 데이터 공급자(임시복사+SetDataProvider deferral) 동일 이식. DragStarting엔 Cancel이 없어 미저장 그룹은 데이터 미설정(no-op)+안내. 핀 드롭 페이로드 불변. 빌드 0/0, 테스트 80/80. (드래그/드롭/핀 실제 동작은 GUI 수동 검증.)
- 2026-06-02: 데이터 저장을 그룹별 폴더 구조로 변경 — `WorkGroup\Groups\{groupId}\`(아이콘=`Icons\{id}.ico/.png`, 바로가기=`{이름}.lnk`). 그룹 삭제 시 폴더째 제거, 고아 정리도 폴더 단위. 폴더 키=그룹 id(GUID), 마이그레이션 없음(기존 그룹은 재저장 시 새 구조 생성·기존 flat 파일/핀은 수동 정리). WorkGroupPaths(GroupsDirectory/GroupIconsDirectory, 옛 Icons/Shortcuts 제거)·ShortcutService(그룹 폴더 .lnk)·GroupAppService(폴더 단위 저장/삭제/고아정리)·GroupIconLoader·DI 변경. 인터페이스/직렬화 불변. 빌드 0/0, 테스트 80/80.
- 2026-06-02: 그룹 아이콘 .ico+.png 동시 생성, 목록은 .png로 표시(선명도 근본 해결) — IconService가 {id}.ico(작업표시줄)와 함께 {id}.png(원본 해상도, EncodePngAsync no-scale)를 저장(CreateGroupIconAsync/CreateDefaultAsync). GroupIconLoader.GetPngPath 추가. GroupListItem은 PNG 우선 로드(없으면 .ico 폴백, 둘 다 IgnoreImageCache)→ .ico 프레임 디코드 없이 GPU 축소로 선명. GroupAppService 삭제/고아정리에 .png 포함. IconServiceTests에 PNG 생성 검증 추가. 기존 그룹은 재저장 시 .png 생성됨. 빌드 0/0, 테스트 80/80.
- 2026-06-02: 그룹 목록 .ico 표시 방식 수정(선명도) — DecodePixelWidth=32가 .ico의 작은 프레임 선택/업스케일을 유발해 흐림. 크기 미지정 디코드(최대 프레임)→Image(32px) GPU 축소로 변경(편집 미리보기와 동일 방식). IconService는 .ico 프레임을 큰 것부터 기록(frame0=최대). 참고: 기존 그룹 .ico는 옛 파이프라인 산출물이라 그룹 수정→확인으로 재저장해야 개선된 .ico로 재생성됨(수동, 사용자 결정). 빌드 0/0, 테스트 80/80.
- 2026-06-02: 작업 그룹 목록 아이콘을 다시 .ico로 표시 — .ico 생성 선명도 개선(업스케일 금지+종횡비 보존) 후, 목록도 모든 그룹을 생성된 .ico로 통일(앞서 넣은 CustomImage 원본 직접 로드 분기 제거). GroupListItem.TryLoadCustomImage 제거. .ico 로드 시 DecodePixelType.Logical + IgnoreImageCache는 유지(선명/즉시반영). 빌드 0/0.
- 2026-06-02: .ico 생성 선명도 개선 — 기존엔 모든 프레임을 size×size로 stretch해 ① 작은 원본(~100px)을 256으로 업스케일(흐림) ② 비정사각 원본 왜곡. 변경: 원본 최대 변 이하의 표준 크기(16/24/32/48/64/128/256)만 생성 + 최상위는 원본 크기(업스케일 금지), 각 프레임은 종횡비 보존 축소(Fant) 후 정사각 투명 캔버스 중앙 배치. IconService.WriteIcoAsync/EncodeFrameAsync/ScaleAsync 추가. 프레임 수 가변이라 IconServiceTests의 프레임 수 단정(4)→(≥1)로 완화. 빌드 0/0, 테스트 80/80(.ico 생성·디코드 테스트 포함).
- 2026-06-02: 작업 그룹 목록 그룹 아이콘 선명도 — 목록이 생성된 .ico(32/48 프레임, 미리 축소돼 흐림)를 쓰던 것을, CustomImage(리소스/사용자 이미지) 그룹은 원본을 네이티브 해상도로 직접 로드(편집 미리보기와 동일). GroupListItem.TryLoadCustomImage 추가(ms-appx 절대 URI/사용자 파일, IgnoreImageCache). BuiltIn/MemberApp은 기존 .ico 유지. 빌드 0/0.
- 2026-06-02: 작업 그룹 목록 그룹 아이콘 3종 버그 수정 — ① 수정 후 미반영: 같은 .ico 경로 재로드 시 WinUI 이미지 캐시가 옛 아이콘 반환 → `CreateOptions=IgnoreImageCache`. ② 잘림: Border(44/Padding4/테두리1) 내부 영역(≈34)보다 Image(36)가 커서 클리핑 → Image 36→32(Stretch=Uniform). ③ 흐림: DecodePixelWidth=32 디코드 후 확대 → `DecodePixelType=Logical`(+DecodePixelHeight)로 DPI 기준 디코드(.ico 48/256 프레임 활용). GroupListItem.cs/WorkGroupsPage.xaml. 빌드 0/0.
- 2026-06-02: 작업 그룹 헤더-목록 간격 축소 — InfoBar가 별도 행이라 RowSpacing(12)이 이중 적용되던 문제. InfoBar를 콘텐츠 그리드 안으로 이동하고 외곽 행을 3→2개로 축소, 메시지 없을 때 InfoBar를 접도록 StatusVisibility 추가(WorkGroupsViewModel). 헤더→목록 간격 ≈24→12px. 빌드 0/0.
- 2026-06-02: 그룹 삭제 시 상단 안내 메시지 제거 — WorkGroupsViewModel.DeleteAsync의 StatusMessage 설정 삭제(InfoBar는 다른 메시지에 계속 사용). 빌드 0/0.
- 2026-06-02: 작업 그룹 헤더 개편 — "그룹 추가" 버튼을 설명글 아래 줄로 이동하고, 같은 줄 왼쪽에 등록 그룹 수 표시(예: "3 그룹"). WorkGroupsViewModel.GroupCountText 추가(LoadAsync에서 OnPropertyChanged 통지), WorkGroupsPage.xaml 헤더를 제목/부제 + [개수·추가] 2단 구성으로 변경. 빌드 0/0.
- 2026-06-02: 작업 그룹 목록 멤버 앱 아이콘에 앱 이름 툴팁 — 멤버 아이콘 타일에 `ToolTipService.ToolTip="{x:Bind DisplayName}"` 바인딩(마우스 오버 시 앱 이름). WorkGroupsPage.xaml만 변경. 빌드 0/0.
- 2026-06-02: 작업 그룹 목록 아이콘에 사각 테두리 — 그룹 아이콘 Border에 테두리선(CardStrokeColorDefaultBrush, 1px) 추가, 멤버 앱 미니 아이콘을 둥근 사각 타일(30×30, CornerRadius 6, 보조 배경+테두리)로 감쌈. WorkGroupsPage.xaml만 변경(바인딩/로직 불변). 빌드 0/0.
- 2026-06-02: "앱 추가" 팝업 선택 체크 + 토글 — 추가된 앱을 팝업에서 제외하던 동작을 변경해, 목록에 남기고 항목 앞에 체크 아이콘 표시(LaunchTarget 기준). 항목 클릭 시 추가↔해제 토글(ToggleApp). PopupAppItem.IsSelected/SelectionGlyphVisibility 추가, RefreshAvailable 전체표시+IsSelected 계산, 픽커 DataTemplate에 고정폭 체크 열, OnAppPickerItemClick→ToggleApp. 본문 선택 목록/삭제는 현행 유지. 공개 API/도메인 불변. 빌드 0/0, 테스트 80/80.
- 2026-06-02: Win32 앱 아이콘도 셸 렌더로 전환 — 설치 목록의 Win32 .lnk(콘솔/스크립트: Strawberry Perl CPAN 도구, VS 개발자 명령 프롬프트 등) 아이콘 누락 해결. `GetThumbnailAsync`(.lnk에서 자주 실패) 대신 `IShellItemImageFactory`를 파일 경로에도 적용(DevDashboard 동일, 탐색기/시작 메뉴 아이콘). `PackagedAppIcon`→`ShellIcon` 일반화: `OpenForAppAsync(AppEntry,size)`가 packaged=shell:AppsFolder\AUMID, Win32=파일 경로로 추출. 크기 캐스케이드(256/128/64/48/32)+플래그 폴백(ICONONLY→일반) 추가. IconService/AppIconLoader는 모든 Kind에 셸 아이콘 우선, 실패 시 기존 폴백. 빌드 0/0, 테스트 80/80. (시각은 GUI 수동 검증.)
- 2026-06-02: 패키지 앱 아이콘 셸 렌더 전환 — `GetLogo`는 앱별 매니페스트 로고 여백 편차로 아이콘 크기가 들쭉날쭉(targetsize-unplated 에셋만 둔 앱은 작게 보임). `PackagedAppIcon.OpenIconStreamAsync`를 `shell:AppsFolder\{AUMID}` + `IShellItemImageFactory.GetImage`(SIIGBF_BIGGERSIZEOK|ICONONLY)로 재작성 → 시작 메뉴와 동일한 균일 아이콘. HBITMAP(DIBSection)→`SoftwareBitmap`(BGRA8 premultiplied)→PNG 스트림(System.Drawing 미사용, CsWin32 P/Invoke). NativeMethods.txt에 SHCreateItemFromParsingName/IShellItemImageFactory/GetObject/DIBSECTION/DeleteObject 추가. 호출처(IconService/AppIconLoader) 메서드명만 갱신, 분기·폴백 로직 불변. 새 NuGet 의존성 없음. 빌드 0/0, 테스트 80/80. (셸 아이콘 시각 정합은 GUI 수동 검증.)
- 2026-06-02: 패키지 앱 아이콘 추출 개선(GetLogo) — UWP/Store 앱(`package.Logo` 경로 없음)의 아이콘 누락 해결. 신규 `PackagedAppIcon.OpenLogoStreamAsync`(AUMID→`PackageManager`→`AppListEntry.DisplayInfo.GetLogo`→`IRandomAccessStream`, OS 19041 가드, 실패 null). `IconService.ResolveMemberBitmapAsync`(그룹 .ico)·`AppIconLoader.LoadAsync`(팝업/미니 아이콘) 양쪽에 Packaged 분기로 GetLogo 우선 적용, 실패 시 기존 폴백 유지. 스트림 디코드 `DecodeStreamAsync` 공통화. WinRT 네이티브(새 의존성 없음), 공개 API/직렬화 무변경. 빌드 0/0, 테스트 80/80.
- 2026-06-02: 그룹 편집 다이얼로그 성능 버그 수정(근본 원인) — InitializeAsync가 오픈 시 설치앱 전체 인벤토리 + 수백 개 셸 썸네일 아이콘을 eager 로드해 UI 스레드 포화(멤버 목록 지연 + 취소 지연). 편집 멤버는 group.Apps에서 즉시 복원(인벤토리 무관), 설치앱 인벤토리는 "앱 추가" Flyout 열 때, 리소스 아이콘 그리드는 아이콘 Flyout 열 때 지연 로드(1회 가드 + ProgressRing). 빌드 0/0, 테스트 80/80. (근본 원인·가설은 plan.md Debug 섹션.)
- 2026-06-02: 그룹 추가/수정 다이얼로그 전면 개편 — 상단 [아이콘 미리보기][이름(15자)], 아이콘 클릭 시 사용자 이미지/리소스 아이콘 선택(단일 Flyout 토글 + GridView), "앱 추가" Flyout로 설치 앱을 선택 목록에 추가(항목별 삭제), 확인 시 빈 목록·이름 중복 검증(GetAllAsync 스냅샷). 기존 ComboBox/체크박스 모델 제거(SelectableAppItem 삭제, PopupAppItem 재사용). 리소스 아이콘 91개 번들(Assets/GroupIcons) + IconService ms-appx 디코드 + ResourceIconCatalog. 도메인/직렬화 무변경(리소스=CustomImage ms-appx URI). 빌드 0/0, 테스트 80/80.
- 2026-06-02: 작업 그룹 목록을 항목별 개별 카드로 표시(SettingsCard 목록과 동일한 룩) — 단일 ListView 카드 래퍼 제거, 각 GroupListItem을 둥근 카드 Border(간격 8)로, ListViewItem 패딩 제거(전체 폭). 드래그 핀/수정·삭제/바인딩 계약 불변. 빌드 0/0, 테스트 80/80.
- 2026-06-02: WinUI Gallery Fluent 디자인 정합(참조: NapCat) — CommunityToolkit.WinUI.Controls.SettingsControls 8.2.251219 추가(WinAppSDK 1.8 호환). 디자인 토큰 리소스(Resources/Spacing.xaml=PageContentPadding/ContentMaxWidth/SideNavWidth, ControlStyles.xaml=CardStyle/Hero/Primary·SecondaryActionStyle/SettingsGroupHeaderStyle). 셸: 커스텀 TitleBar(ExtendsContentIntoTitleBar) + NavigationView 정교화(280 pane, 컨텐츠 보더 제거, Transparent). 설정/정보 페이지를 SettingsCard로, 작업그룹/트레이/정보/설정 전 페이지를 공통 레이아웃(ScrollViewer/PageContentPadding/MaxWidth/헤더)으로 통일. 기능/바인딩 불변(코드비하인드 미변경). 빌드 0/0, 테스트 80/80. (TitleBar 드래그/캡션 시각은 GUI 수동 확인 대상.)
- 2026-06-02: WinUIEx 2.9.1 재도입 — 메인 창을 WindowEx로 전환해 창 크기/위치 지속(PersistenceId="WorkGroupMain")·최소 크기(800×560) 관리. Mica는 표준 SystemBackdrop 유지(WinUIEx 자체 Backdrop은 CS0618 deprecated). 트레이 종료 시 `_window.Close()`로 persistence 저장 보장(닫기→트레이 숨김 구조에서 Window.Closed 발생 시점 확보). 트레이는 WinUIEx 미지원이라 Win32 Shell_NotifyIcon 유지. 빌드 0/0, 테스트 80/80.
- 2026-06-02: UI 전면 개편(Plan 3, T1~T8) — NavigationView 셸(MainShell: 작업 그룹/트레이 메뉴/설정/정보) + Fluent(Mica 백드롭). 설정 페이지(자동 시작 토글 + 테마 시스템/다크/라이트, ThemeService LocalSettings 영속). 정보 페이지(버전 + 오픈소스 라이선스 목록). 트레이 메뉴 placeholder. 그룹 추가/수정은 ContentDialog(설치앱 체크박스 선택 + 아이콘 설정 + 미리보기). 작업 그룹 페이지(그룹 아이콘 + 2라인[이름/멤버 아이콘] + 수정/삭제 아이콘 버튼 + 드래그 핀 + 삭제 확인). 단일 MainPage/MainViewModel 제거. 빌드 0/0, 테스트 80/80.
- 2026-06-02: T12 — 자동 시작(StartupTask + 토글) + Win32 트레이 아이콘(Shell_NotifyIcon, 열기/종료) + 실행 별칭 `WorkGroupSpike.exe`→`WorkGroup.exe` finalize + README/notes 갱신. 빌드 0/0.
- 2026-06-02: T11 — 정식 팝업 런처(GroupPopupWindow, 아이콘 그리드 + 클릭 실행) + IAppLauncher + AppIconLoader. spike 팝업 제거.
- 2026-06-02: T9+T10 — 메인 관리 화면(MainViewModel/MainPage) + 그룹 드래그 등록(검증된 패턴) + DI 조립(ServiceConfiguration/WorkGroupPaths).
- 2026-06-01: T2 배포 수정 — 앱을 MSIX 패키지 모드로 전환(`WindowsPackageType=None` 제거). VS F5 배포 시 DEP1700(.appxrecipe 없음) 해결. 빌드 0/0, .appxrecipe 생성 확인.
- 2026-06-01: T1b — 레이어 프로젝트(Domain/Application/Infrastructure) + 테스트 2개(xUnit) 생성, 프로젝트 참조 연결, 솔루션 등록. AGENTS.md/README.md/notes.md 작성. 전체 빌드 0/0, 테스트 2/2 통과.
- 2026-06-01: T1a — WinUI3 앱(`WorkGroup.App`, net10.0-windows10.0.19041.0) + `WorkGroup.slnx` 스캐폴딩. `dotnet build WorkGroup.slnx` 0/0 검증. git 저장소 초기화.
