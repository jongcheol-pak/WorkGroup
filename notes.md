# 작업 노트

## 최근 변경
- 2026-06-05: 작업 그룹 화면에 그룹 검색 기능 추가(plan T1~T4) — 트레이 메뉴의 폴더 검색(FolderShortcutsViewModel/TrayMenuPage) 패턴을 작업 그룹 화면에 동일 이식. ① WorkGroupsViewModel: 전체 원본 `_all` 리스트 + `SearchText`(ObservableProperty)·`OnSearchTextChanged`→`ApplyFilter` 추가, 검색은 **그룹 이름 또는 멤버 앱 이름(`Group.Apps[].DisplayName`)** 부분일치(OrdinalIgnoreCase), `LoadAsync`가 `_all` 재구성 후 `ApplyFilter`로 표시용 `Groups` 채움. `GroupCountText`를 `Groups.Count`→`_all.Count` 기준으로(검색 중에도 전체 개수 유지, 폴더 `FolderCountText`와 동일 정책), `IsEmpty = _all.Count == 0`(검색 무매치는 빈 목록만, 전체 0개일 때만 빈 상태 안내 — 폴더와 동일). ② WorkGroupsPage.xaml: 제목 아래·목록 위에 검색 `TextBox`(PlaceholderText=`WorkGroups_SearchPlaceholder`, `SearchText` TwoWay+PropertyChanged) 추가, 헤더 Spacing(8)·개수 Grid Margin 유지. ③ 외부 "그룹 수정" 라우팅(WorkGroupsPage.xaml.cs `EditGroupByIdAsync`)이 검색 필터가 켜져 있어도 대상을 찾도록, 필터된 `Groups` 대신 원본 전체 조회로 보정 — 품질 리뷰 M1 반영해 내부 `_all` 노출 대신 `WorkGroupsViewModel.FindByIdAsync(id)`(미로드면 1회 LoadAsync 후 `_all`에서 조회) 메서드로 캡슐화, 코드비하인드 로드가드+FirstOrDefault 2줄을 1줄로 단순화. ④ 신규 리소스 키 `WorkGroups_SearchPlaceholder` 4개 언어(ko"그룹 검색...", en"Search groups...", ja"グループを検索...", zh"搜索群组..."). 직렬화/도메인/리포지토리 무변경, 공개 멤버는 추가만(제거·시그니처 변경 없음). 빌드 0/0(클린 리빌드), 테스트 Domain 23 / Application 94. 리뷰: spec OK(0/0/0), quality MAJOR 1(AllGroups 캡슐화)→반영. 검색창 표시·이름/멤버앱이름 필터·개수 유지·빈 검색 복원은 F5 MSIX GUI 수동 검증 대상.
- 2026-06-05: 로그인 시 자동 시작 토글 기본값을 켜짐(on)으로 변경(Package.appxmanifest) — 토글 값은 별도 저장 없이 OS `StartupTask` 상태를 직접 읽으므로(`StartupService.IsEnabledAsync`), `windows.startupTask` 확장의 `StartupTask Enabled="false"` → `Enabled="true"`로 변경(주석 "기본 비활성"→"기본 활성"도 동기화). 신규 설치 시 OS가 StartupTask를 Enabled 상태로 시작 → 설정 화면 토글이 기본 켜짐으로 표시. 빌드 0/0(`-p:Platform=x64 -p:RuntimeIdentifier=win-x64`).
- 2026-06-05: Release 빌드 시작 즉시 크래시 수정(Package.appxmanifest + WorkGroup.App.csproj) — 증상: Debug 정상, **Release(self-contained publish)로 만든 loose exe 실행 시 시작 즉시 크래시**. 실제 예외(이벤트 로그 .NET Runtime 1026): `COMException 0x80040154 REGDB_E_CLASSNOTREG` @ `WindowsAppRuntime.DeploymentManager.AutoInitialize.AccessWindowsAppSDK`. **근본 원인**(정상 동작 프로젝트 NapCat과 대조로 확정): `Package.appxmanifest`의 `windows.startupTask` 확장이 `Executable="$targetnametoken$.exe"`/`EntryPoint="$targetentrypoint$"` 토큰을 사용 — 이 토큰은 **메인 `<Application Executable=>`에서만 치환되고 Extensions 안에서는 치환되지 않아** MakeAppx가 "$targetnametoken$.exe 파일 없음"으로 검증 실패 → **MSIX 패키지가 생성되지 못함** → 패키지 대신 loose self-contained exe만 남고, 그 exe에는 WinAppSDK 런타임 프레임워크가 없어 DeploymentManager 자동 초기화가 REGDB_E_CLASSNOTREG로 죽음. 폐기한 가설: WindowsAppSDK 1.*→2.1.3 버전(NapCat도 2.1.3, 무관), PublishTrimmed(꺼도 동일). 수정: ① startupTask 확장의 `Executable="WorkGroup.App.exe"` + `EntryPoint="Windows.FullTrustApplication"`로 리터럴 지정(NapCat 동일 패턴, TaskId/DisplayName 유지). ② csproj에 빠져 있던 MSIX 패키징 속성 추가(GenerateTemporaryStoreCertificate/AppxPackageSigningEnabled/AppxPackageSigningTimestampDigestAlgorithm/AppxAutoIncrementPackageRevision/AppxBundle/GenerateAppInstallerFile/GenerateTestArtifacts). 검증: `-p:GenerateAppxPackageOnBuild=true`로 `WorkGroup.App_1.0.0.0_x64.msix` 생성 성공, 레이아웃 매니페스트를 Add-AppxPackage -Register로 등록 후 패키지 앱 실행 시 **DeploymentManager REGDB 지점을 통과해 설치된 WinAppSDK 런타임(WindowsAppRuntime.2_2.1.3.0) 정상 로드** 확인(원래 크래시 해소). 일반 Release 빌드 0 error 유지. 참고: 자동화(헤드리스) 세션에서 GUI를 셸로 띄울 때 보이는 XAML `0xc000027b` 크래시는 Debug에서도 동일 재현되는 테스트 하네스 아티팩트로 Release 전용 이슈 아님 — 실제 사용 GUI 실행은 별도 수동 검증 대상.
- 2026-06-04: 폴더 2차+ 팝업을 작업 표시줄 변에 따라 좌/우 고정 배치(FolderListPopupWindow/FolderContentsPopupWindow) — 증상: 작업 표시줄이 왼쪽일 때 트레이 좌클릭 폴더 팝업의 하위(2차) 팝업이 왼쪽으로 떠 이전(1차) 팝업을 가림. 원인: `PlaceAtAnchor`가 "부모 왼쪽 우선 → 공간 없으면 오른쪽 → 작업영역 우변 클램프"라, 좁은 공간에서 우변 클램프가 자식을 왼쪽으로 밀어 부모를 가림. 수정: `ChildPopupAnchor`에 `TaskbarEdge Edge` 추가(1차 `FolderListPopupWindow`가 `TaskbarPopupPositioner.DetectEdge(_metrics.Monitor, _metrics.Work)`로 계산해 전달, 2차→3차는 `BuildChildAnchor`로 전파). `PlaceAtAnchor`에서 Edge가 **Left면 자식을 항상 부모 오른쪽(공간 무관·클램프 안 함), Right면 항상 부모 왼쪽**, 상/하·미지정은 기존 로직 유지. 호출처 2곳(ShowChildPopup/BuildChildAnchor) 함께 수정, 빌드 0/0·테스트 117/117. 좌/우 작업 표시줄에서 하위 팝업이 항상 바깥쪽으로 펼쳐지는지 F5 GUI 검증 대상.
- 2026-06-04: 세로 핀 팝업 레이아웃 최종 조정 완료(GroupPopupWindow, GUI 검증됨) — ① 아이콘 잘림: `GridView.ItemContainerStyle`(`PopupItemContainerStyle`)로 GridViewItem 기본 여백(Padding/Margin/MinWidth/MinHeight) 0 + HorizontalContentAlignment/VerticalContentAlignment=Center → 아이템 폭을 콘텐츠(48px 박스)로 확정. 사라진 아이콘 간격은 가로/세로 `ItemsPanelTemplate` StackPanel `Spacing="4"`로 보존. ② 좌우 여백: `VerticalIconColumnWidth`=80(아이콘 48 중앙 + 좌우 16, 가로 모드처럼 여백 확보). ③ 하단 여백: 루트 Grid `Padding`이 `"12,12,12,0"`(하단 0)이라 세로 1열 마지막 아이템이 하단에 붙음 → 생성자에서 `_isVertical`일 때 `rootGrid.Padding`을 하단도 좌우와 같게(12) 설정. 가로 경로는 모두 무영향. 빌드 0/0, 테스트 117/117.
- 2026-06-04: 세로 핀 팝업이 안 줄던 **확정된 진짜 원인 = OS 최소 창 너비** → WM_GETMINMAXINFO 직접 가로채 해결(GroupPopupWindow). 헤더 측정 제외 진단으로 `fw72 ww94 aw198` 확보: 측정·요청폭은 72/94로 정상인데 **OS가 198px로 강제 적용**(aw≫ww) — `SetBorderAndTitleBar(true,false)` 표준 테두리 창의 OS 기본 최소 추적 크기. 앞서 시도한 WinUIEx `WindowManager.MinWidth=1`은 실제로 override가 안 먹었음(되돌림). 해결: 생성자에서 `_isVertical`일 때 `RemoveMinimumTrackSize()` — `comctl32` `SetWindowSubclass`로 창을 서브클래스해 `WM_GETMINMAXINFO`(0x0024)에서 `MINMAXINFO.ptMinTrackSize.X/Y=1`로 낮춤(Marshal 사용, 델리게이트는 필드 보관해 GC 방지, 창 파괴 시 자동 정리). 이제 ww(94)대로 적용돼 세로 팝업이 콘텐츠 폭으로 줄어든다. 빌드 0/0, 테스트 117/117. 좌/우 세로 팝업 72~94px 축소·호버 박스 정상화 F5 GUI 검증 대상. (헤더 숨김·고정 아이템 폭 산출은 아래 항목 유지.)
- 2026-06-04: 세로 핀 팝업 너비 과대의 원인 분석 중 = 그룹 이름 헤더 → 세로 모드에서 헤더 숨김(GroupPopupWindow). **진단으로 확정**: 팝업에 `fw`(측정폭)/`ww`(요청폭)/`aw`(OS 실제적용폭) 임시 표시 → 값이 `fw398 ww420 aw420 ivTrue`. (1) `aw == ww`라 **OS는 요청 너비를 그대로 적용**(앞서 의심한 OS 최소 너비 제약은 오진 → WinUIEx `WindowManager` 코드·using 되돌림). (2) `fw=398`은 `398-24(패딩)=374=헤더(TitleText) 폭` — 즉 **그룹 이름 헤더가 너비를 키운 것**(진단 텍스트가 헤더 자리에 들어가 374가 된 것도 이를 입증). 해결: `LoadAsync`에서 `showHeader = group.ShowPopupHeader && !_isVertical`로 **세로 1열에서는 그룹 이름 헤더를 항상 숨김**(에러 메시지 경로는 그대로 표시). 헤더가 빠지면 `MeasureVerticalContent`의 콘텐츠 폭 = `VerticalIconColumnWidth`(48)+패딩(24)=72px. 빌드 0/0, 테스트 117/117. 좌/우 세로 팝업이 72px로 줄고 호버 박스도 아이콘에 맞는지 F5 MSIX GUI 수동 검증 대상.
- 2026-06-04: 세로 핀 팝업 너비 과대 표시 버그 수정(GroupPopupWindow) — 증상: 작업 표시줄 좌/우 변에서 핀 팝업을 세로 1열로 표시할 때 너비가 콘텐츠(48px 아이콘)보다 훨씬 넓게 뜸(가로는 정상). **근본 원인**: `MeasureVerticalContent`가 **GridView(`AppsGrid`)를 측정에 사용**한 것. GridView(내부 ScrollViewer)는 measure 시 주어진 가용 폭을 그대로 `DesiredSize`로 반환해 콘텐츠 폭을 얻을 수 없음. 시도 경과: ① `HorizontalAlignment=Left` — arrange만 바꿔 measure `DesiredSize`·창 크기에 무영향(아이콘만 왼쪽 정렬, 창은 넓음). ② 내부 패널 `ItemsPanelRoot` 직접 measure — 이미 가용 폭으로 arrange된 라이브 트리라 콘텐츠 폭을 못 얻음(아이콘만 가운데 정렬, 창은 여전히 넓음). **최종 수정(측정 비의존)**: 세로 1열 아이템은 모두 **48px 고정 박스**(`AppItemTemplate`/`AddButtonTemplate` `Width="48"`)라는 사실을 이용해 아이템 영역 폭을 고정 상수 `VerticalIconColumnWidth`(64)로 잡고, **가변인 헤더(`TitleText`)만 측정**(TextBlock이라 measure 정확)해 둘 중 큰 값을 콘텐츠 폭으로 사용 → 루트 Grid 좌우 패딩(`VerticalContentSidePadding`=24) 가산 → `maxWidth` 상한 클램프. 확정 폭으로 `root` 높이만 측정(높이는 배치축이라 GridView 측정 정확). 세로 스크롤 시 `VerticalScrollBarWidth`(16) 가산. 결과 너비 = max(64, 헤더폭)+24 → 헤더 없으면 88px 고정(측정 비의존이라 결정적). 빌드 0/0, 테스트 117/117. Follow-up: 아이콘 박스 chrome에 따라 `VerticalIconColumnWidth`(64) 미세조정 가능(GUI 확인 후), `VerticalScrollBarWidth` 하드코딩 근사. 좌/우 세로 너비는 F5 MSIX GUI 수동 검증 대상(헤드리스 불가).
- 2026-06-04: 작업 표시줄 위치별 핀 그룹 팝업 방향 전환(plan T1~T3) — 작업 표시줄이 좌/우 변에 붙으면 핀 그룹 팝업(GroupPopupWindow) 아이콘을 **세로 1열**로, 상/하면 기존대로 **가로 1행**으로 표시. 위치 계산(`TaskbarPopupPositioner`)은 이미 4변(상/하/좌/우)을 모두 처리하므로 무변경, 핵심은 ① 방향 전환 ② 측정·스크롤 분기. 구현: ① XAML — 인라인 `GridView.ItemsPanel`을 제거하고 가로/세로 `ItemsPanelTemplate`(StackPanel Orientation=Horizontal/Vertical)을 `Grid.Resources`에 `HorizontalItemsPanel`/`VerticalItemsPanel` 키로 분리. ② 생성자 — `_metrics` 캡처 직후(LoadAsync 전) `TaskbarPopupPositioner.DetectEdge`로 변 판정 → `_isVertical`(readonly) = (Left|Right) → `AppsGrid.ItemsPanel`을 해당 리소스로 1회 교체(아이템 로드 전이라 ItemsHost 재생성·깜빡임 없음). ③ `AdjustToContent`를 `MeasureHorizontalContent`(현행 로직 보존)·`MeasureVerticalContent`(신규)로 분기, 공통 후처리(chrome 보정·`_lastApplied` 가드·`_popupWidth` 갱신·Resize·Move) 유지. 세로 경로: 양축 스크롤 Disabled로 측정 → 높이 상한(`Work.Height - WorkAreaMargin`) 클램프·초과 시 세로 스크롤 Auto+UpdateLayout → 확정 높이로 너비 재측정 → 너비도 상한(`Work.Width - WorkAreaMargin`) 클램프(좁은 좌/우 작업영역 화면 밖 이탈 방지). `_popupWidth = windowWidth` 갱신이 우측 변 우측 정렬(`Compute`의 `work.Right - popupWidth`)에 필수. 빌드 0/0, 테스트 117/117(위치 계산 회귀 가드). 리뷰: spec OK, quality MAJOR 2 중 세로 측정 UpdateLayout 반영·`MoveToTaskbar` 시그니처 비대칭은 follow-up. 좌/우/상/하 실제 방향·세로 스크롤·우측 정렬·빈 여백 없음은 F5 MSIX GUI 수동 검증 대상(헤드리스 불가).
- 2026-06-04: 핀 팝업(GroupPopupWindow) 앱 목록 끝에 "+" 그룹 편집 버튼 추가(plan T1~T4) — 핀 클릭 팝업의 앱 아이콘 목록 마지막에 "+" 버튼을 한 항목으로 두고, 클릭 시 우클릭 메뉴 "그룹 수정"과 동일하게 메인 창의 해당 그룹 편집 다이얼로그를 연다. 구현: ① 마커 클래스 `PopupAddButtonItem`(빈 sealed) + `PopupGridItemTemplateSelector`(DataTemplateSelector, `item is PopupAddButtonItem ? AddButtonTemplate : AppTemplate`, 두 SelectTemplateCore 오버로드 모두 구현) 신규. ② GroupPopupWindow.xaml: 인라인 ItemTemplate을 `GridView.Resources`의 `AppItemTemplate`(x:Key)로 분리하고, "+" `AddButtonTemplate`(앱 항목과 동일 48×48 박스 2중 구조 + 중앙 FontIcon `&#xE710;` + ScaleTransform 호버 + 툴팁 `GroupPopup_EditGroup` 재사용, ContextFlyout 없음) 추가, GridView를 `ItemTemplateSelector`로 전환. ③ GroupPopupWindow.xaml.cs: `Items`를 `ObservableCollection<object>`로 확장(앱+추가버튼 혼합), 정상 그룹 로드 시 멤버 추가 직후 목록 끝에 `PopupAddButtonItem` 1개 주입(NotFound `return`/예외 catch 경로에는 미추가 → 멤버 0개 정상 그룹은 버튼만 단독 표시), `OnAppClick`에 추가버튼 분기, `OnEditGroupClick` 본문(Close 포함)을 `EditGroup()` 공통 메서드로 추출해 우클릭/추가버튼 공유. 신규 리소스 키 없음(기존 `GroupPopup_EditGroup` 4개 언어 재사용). 측정 로직(AdjustToContent)은 버튼이 GridView 내부 마지막 항목이라 한 줄 측정에 자연 포함되어 무변경(오버플로 시 함께 가로 스크롤). 빌드 0/0, 테스트 117/117. 버튼 표시·호버·클릭→편집 다이얼로그는 F5 MSIX GUI 수동 검증 대상.
- 2026-06-04: 전체 다국어화(i18n) + 설정 언어 변경 기능 추가(plan T1~T11) — 앱의 모든 하드코딩 문구(약 95개 App + 16개 Infra)를 리소스(.resw)로 추출해 **한국어/영어/일본어/중국어(간체) 4개 언어** 제공. 메커니즘: ① `LocalizationService`(MRT Core `ResourceManager`+직접 구성한 `ResourceContext`로 언어 한정자 명시 제어) + `ILocalizer`(Application 추상화)/`NullLocalizer` 폴백 + `{loc:Localize Key=...}` 마크업 익스텐션(XAML 직접/부착 속성·Window 루트 모두 동작, PoC 빌드 게이트 통과). ② `LanguageService` — LocalSettings "AppLanguage"에 선택값(System/ko-KR/en-US/ja-JP/zh-Hans) 영속, 시작 시 `PrimaryLanguageOverride`(셸/매니페스트용)+`LocalizationService` 한정자(인앱용) 동시 적용, 변경은 `AppInstance.Restart`로 적용(D2 재시작 방식). ③ 설정 화면에 "언어" SettingsCard+ComboBox(기본 "시스템 언어", 시스템 항목만 로컬라이즈·나머지 endonym), 변경 시 재시작 확인 다이얼로그(ConfirmRestartAsync 콜백). ④ XAML 11개 파일 + ViewModel/코드비하인드/TrayIconService 문구 치환(VM=DI 주입, 코드비하인드/Tray/마크업=static `Current`). ⑤ Infra 6개 서비스(ShortcutService/AppLauncher/InstalledAppInventory/IconService/2 Repository)에 `ILocalizer? = null` 선택 인자 주입(NullLocalizer 폴백, ILogger? 패턴 동형), `Result.Fail`/`.lnk` 설명 로컬라이즈, ServiceConfiguration에서 6개 모두 ILocalizer 명시 전달. ⑥ `Package.appxmanifest`의 DisplayName/Description를 `ms-resource:///Resources/App_DisplayName`·`App_Description`로(시작 메뉴/앱 목록 표시 이름 다국어화). ⑦ 앱 표시 이름: ko=작업 관리/en=WorkGroup/ja=ワークグループ/zh=工作组. resw 누적 122키×4언어. 검증: 빌드 0/0, 테스트 117/117(신규 `ResourceParityTests` 2 — 4개 언어 키 패리티/빈 값 검사), `makepri dump`로 클린 빌드 PRI에 4개 언어 키가 `Resources` 맵으로 정상 컴파일 확인(`ms-resource://{pkg}/Resources/App_DisplayName`). **주의: 증분 빌드는 신규 resw를 PRI에 재생성하지 못함 → resw 변경 후 클린 빌드 필요**(VS F5 배포는 대개 정상). 런타임 언어 전환·셸 표시 이름은 F5 MSIX GUI 수동 검증 대상. Domain 레이어 Result.Fail 한국어("그룹 이름은 필수입니다." 등)는 App 검증에 가려지는 방어용이라 범위 외(plan Known Workarounds, 근본 해결은 에러 코드 방식·별도 승인 필요).
- 2026-06-04: 그룹 팝업 우클릭 메뉴(GroupPopupWindow.xaml MenuFlyout) 아이콘+구분선 추가 — Windows 표준 컨텍스트 메뉴 스타일로, 각 항목에 MenuFlyoutItem.Icon(FontIcon Segoe Fluent) 추가: 열기=&#xE8A7;(OpenInNewWindow), 관리자 권한으로 실행=&#xE7EF;(Admin 방패), 그룹 수정=&#xE70F;(Edit 연필, 기존 GroupEditDialog 검증 glyph). "그룹 수정" 위에 MenuFlyoutSeparator 추가. 빌드 0/0. 아이콘 표시 F5 MSIX GUI 검증 완료.
- 2026-06-04: 상주 인스턴스 redirect 팝업이 다른 앱 클릭 시 안 닫히는 버그 수정(GroupPopupWindow). 증상: redirect로 띄운 그룹 팝업이 다른 앱/바탕화면 클릭해도 닫히지 않음(콜드 경로는 정상). 근본 원인: GroupPopupWindow는 OnActivated의 Deactivated 감지로 닫는데(299-303행), 콜드 프로세스는 OS가 새 창을 자동 포그라운드로 띄워 Activate()만으로 활성화됐으나 **상주(백그라운드) 프로세스에서 띄우면 Activate()만으론 포그라운드 포커스를 못 잡아** Activated가 안 되고 → Deactivated도 안 와 안 닫힘. FolderListPopupWindow가 트레이 상주 경로라 이미 겪고 SetForegroundWindow로 해결한 것과 동일 현상(FolderListPopupWindow.xaml.cs:383-385 주석). 수정: GroupPopupWindow.RevealAtTaskbar 끝에 `SetForegroundWindow(WindowNative.GetWindowHandle(this))` 호출 + user32 P/Invoke 추가(콜드/상주 공통, 콜드는 이미 포그라운드라 무해). 빌드 0/0, 테스트 115/115. 닫힘 동작 F5 MSIX GUI 검증 완료.
- 2026-06-04: 핀 클릭 팝업 표시 지연 해소 — 상주 인스턴스 redirect 도입(App.xaml.cs 단일 변경). 증상: 핀 클릭 시 매번 새 프로세스가 떠 ServiceConfiguration.Build()(DI 전체)+groups.json 로드+콜드스타트를 반복해 표시가 느림. 해결: 그룹 활성화(`--group`/`workgroup://`)를 **트레이 상주 메인 인스턴스가 있으면 그쪽으로 redirect**해 이미 초기화된 프로세스가 팝업만 즉시 표시. 결정=하이브리드(상주 중이면 redirect, 콜드면 기존처럼 팝업 전용 프로세스로 띄우고 닫히면 종료 — 콜드 동작·트레이 미생성 불변) + 중복 시 이전 팝업 닫고 새로. 구현: ① MainInstanceKey 상수(71행 FindOrRegisterForKey 인자 리터럴과 공유) ② FindMainInstance() — `AppInstance.GetInstances()` foreach + `!IsCurrent && Key==MainInstanceKey` 필터(공식 문서 확인: GetInstances는 GetCurrent 호출한 모든 인스턴스를 자기 포함 반환, 미등록 인스턴스 Key는 빈 문자열 → 콜드 자기 자신 제외). ③ OnLaunched 그룹 분기: 상주 있으면 RedirectActivationTo+Exit, 없으면 기존 콜드 분기 유지. ④ OnAppInstanceActivated에 groupId 분기 추가(editId보다 먼저, 즉시 return)→ShowGroupPopup. ⑤ ShowGroupPopup: _groupPopup 추적(_folderPopup 패턴), 할당→Closed(Exit 없음, ReferenceEquals 정리)→old.Close()→Activate 순서 고정(GroupPopupWindow의 Deactivated 자율 Close와 경합 차단). 빌드 0/0, 테스트 Domain 23 / Application 92(회귀 없음). 실제 redirect·즉시 표시·콜드 동작·커서 위치 배치 F5 MSIX GUI 검증 완료. (기존 follow-up: _keyInstance 필드 데드 코드 의심 — 이번 범위 밖, 별도 검토 필요.)
- 2026-06-03: 핀된 그룹 아이콘 클릭 시 팝업이 늦게 뜨는 문제 수정(GroupPopupWindow) — 증상: 핀 클릭 후 팝업이 느리게 표시. 원인: 팝업 창은 화면 밖(OffScreen −32000)에 생성된 뒤 LoadAsync의 finally에서야 작업 표시줄 정위치로 이동·표시되는데, LoadAsync가 `await Task.WhenAll(iconTasks)`로 **모든 멤버 앱 아이콘 로드(셸 썸네일 I/O, 앱 개수에 비례)가 끝날 때까지 대기**해 표시가 그만큼 지연됨. 핵심: XAML 항목 박스가 고정 48px라 레이아웃 크기는 아이콘 로드 여부와 무관하게 항목 개수만으로 확정 → 아이콘을 기다릴 이유가 없음. 수정: LoadAsync에서 아이콘 await를 제거하고 `_ = item.LoadIconAsync()`로 백그라운드 시작 → 항목 추가 즉시 팝업 표시, 각 아이콘은 OneWay 바인딩으로 로드 완료 시 자기 박스에 채워짐(리사이즈 점프 없음, 시작 메뉴와 유사). 빌드 0/0(x64). 팝업 즉시 표시·아이콘 채움은 F5 MSIX GUI 수동 검증 필요.
- 2026-06-03: 그룹 팝업(GroupPopupWindow) 버그 2건 수정(plan T1·T2) — ① **아이콘 1행 표시**: 기본 GridView(ItemsWrapGrid)가 고정 너비 360에서 아이콘을 여러 행으로 래핑하던 것을, GridView.ItemsPanel을 **비가상화 StackPanel(Orientation=Horizontal)**로 교체해 항상 한 가로줄로 나열. 창 너비는 AdjustToContent에서 동적 측정(무한 너비 측정→작업영역폭 클램프→확정 너비 높이 재측정). **너비 자동맞춤 안 되고 가로 스크롤 생기던 후속 버그 2건 수정**(FolderListPopupWindow 동작 코드 비교로 확정): ⓐ **너비 테두리(hChrome) 미보정** — SetBorderAndTitleBar로 client<window인데 콘텐츠 너비를 창 너비로 그대로 Resize해 client가 좁아져 콘텐츠 넘침→스크롤바. windowWidth=finalWidth+hChrome(=Size.Width−ClientSize.Width)로 보정, _popupWidth도 windowWidth로 저장(배치 정렬). ⓑ **가로 스크롤 활성 상태 측정** — ScrollViewer.HorizontalScrollMode=Auto면 무한 너비 측정 시 자연 너비를 부정확 보고. 측정 동안은 Disabled(정확 측정)로 두고 작업영역 폭 초과 시에만 코드에서 Auto로 전환(XAML 초기값도 Disabled). 너비 자동맞춤·가로 스크롤 미발생 GUI 검증 완료. 창 너비를 고정(PopupWidth 360 const) 대신 동적 측정 — AdjustToContent에서 (a) 너비·높이 무제한 Measure로 1행 자연 너비 산출 → (b) maxWidth=작업영역폭−WorkAreaMargin(24)로 클램프(초과분은 가로 스크롤, ScrollViewer.HorizontalScrollMode/BarVisibility=Auto) → (c) 확정 너비로 높이 재측정(가로 스크롤바 높이 반영, 잘림 방지). PopupWidth const→InitialPopupWidth(360)+_popupWidth 필드(동적)+_lastAppliedWidth 가드(SizeChanged 무한 루프 차단), MoveToTaskbar/초기 Resize도 동적 너비 사용. ② **관리자 권한 실행 복구**: 증상=UAC 확인해도 아무 반응 없음. 원인=MSIX 패키지 앱은 Job Object/패키지 컨텍스트에 묶여 직접 Process.Start(Verb="runas")로 비패키지 자식을 승격하면 UAC 승인 후 프로세스 생성 실패. 해결=AppGroup(검증된 동일 개발자 MSIX 앱) 방식 이식 — AppLauncher.LaunchAsAdmin을 powershell.exe 중개(`-NoProfile -Command "Start-Process -FilePath '{경로}' -Verb RunAs"`, UseShellExecute=false/Hidden/CreateNoWindow)로 교체해 패키지 컨텍스트에서 분리 승격(BuildAdminStartInfo 순수 헬퍼 추출, 작은따옴표 ''이스케이프). powershell 독립 분리로 팝업 Close/Deactivated 타이밍과 무관해져 GroupPopupWindow 변경 불필요. 빌드 0/0, 테스트 Domain 23 / Application 92(BuildAdminStartInfo 신규 2). 1행 레이아웃·폭 상한·가로 스크롤·관리자 승격 실제 동작 F5 MSIX GUI 검증 완료.
- 2026-06-03: 앱 표시 이름을 "WorkGroup" → "작업 관리"로 변경(표시용만). 변경: MainShell.xaml TitleBar Title, AboutViewModel.AppName, TrayIconService szTip(트레이 툴팁), ShortcutService .lnk 설명("{이름} - 작업 관리"), Package.appxmanifest의 Properties DisplayName·VisualElements DisplayName/Description·StartupTask DisplayName. 기술 식별자(네임스페이스 WorkGroup.*, 실행 별칭 WorkGroup.exe, 프로토콜 workgroup, 데이터 폴더 %USERPROFILE%\WorkGroup, PersistenceId/single-instance 키, Win32 클래스명, StartupTask TaskId, 패키지 Identity)는 변경 시 빌드/실행/데이터 경로가 깨지므로 모두 유지. 빌드 0/0. 트레이 툴팁·타이틀·정보 페이지·설정 앱 표시는 F5 MSIX GUI 수동 검증.
- 2026-06-03: 그룹 팝업 "그룹 수정"을 메인 앱 위임 방식으로 교체 + single-instance 도입(plan T1~T7) — 증상: 팝업 내 ContentDialog가 360x~100px 팝업 창 XamlRoot에 갇혀 잘림(ContentDialog는 호스트 창 영역 안에만 렌더). 해결: ① 팝업 OnEditGroupClick이 메인 앱을 "--edit-group {id}"(WorkGroupPaths.AliasExePath 풀패스 = .lnk 타깃) Process.Start로 실행, _editing 가드/팝업 내 다이얼로그 제거(OnActivated 원복). ② GroupArgs.EditFlag("--edit-group")/ParseEditCommandLine/BuildEditCommandLineArguments + ActivationParser.TryGetEditGroupId 추가(공통 ParseFlag로 --group/--edit-group 정확 일치 분리). ③ App.OnLaunched에 single-instance redirect — 메인/편집 활성화만 AppInstance.FindOrRegisterForKey("WorkGroupMainInstance"), !IsCurrent면 RedirectActivationTo(Task.Run+SemaphoreSlim 동기 헬퍼, UI 데드락 회피)+Exit, IsCurrent면 Activated 구독. 팝업(--group)은 redirect 분기 전 처리해 독립 프로세스 유지. ④ Activated(백그라운드)→_uiDispatcherQueue.TryEnqueue 마샬링, RouteEditRequest가 PendingEditGroupId 보관+ShowMainWindow+MainShell.SelectWorkGroups, 이미 로드된 WorkGroupsPage면 즉시 EditGroupByIdAsync 호출. ⑤ WorkGroupsPage.OnPageLoaded가 LoadAsync 후 PendingEditGroupId 소비, EditGroupByIdAsync는 _editDialogOpen 가드(중복 무시)+groupId로 그룹 찾아 기존 ShowEditDialogAsync 재사용. ⑥ 팝업 분기 MainWindow=popup 제거(편집이 메인에서 일어나 picker HWND 불필요). 주의: 커스텀 Program.Main+DISABLE_XAML_GENERATED_MAIN 방식은 WindowsAppSDK 1.8 XAML 컴파일러가 해당 상수만으로 WMC9999 크래시(실측)해 폐기, OnLaunched redirect로 대체. 빌드 0/0, 테스트 Domain 23 / Application 90(GroupArgs edit 파싱 신규 6). single-instance redirect·편집 라우팅·팝업→메인 편집은 F5 MSIX GUI 수동 검증.
- 2026-06-03: 그룹 팝업(GroupPopupWindow) 아이콘 툴팁 + 우클릭 컨텍스트 메뉴 추가(plan T1~T7) — ① 아이콘 StackPanel에 ToolTipService.ToolTip="{x:Bind DisplayName}"로 앱 이름 툴팁. ② 우클릭 StackPanel.ContextFlyout(MenuFlyout) 3항목: "열기"(Launch+닫힘), "관리자 권한으로 실행"(LaunchAsAdmin+닫힘, Win32만 활성 — IsEnabled="{x:Bind CanRunAsAdmin}"), "그룹 수정"(편집 다이얼로그). ③ IAppLauncher.LaunchAsAdmin(AppEntry) 신규 — AppLauncher가 Win32는 ProcessStartInfo.Verb="runas"로 UAC 승격, Packaged는 Process.Start 전 Result.Fail 거부, 예외(UAC 취소 1223 포함)는 LogWarning+Result.Fail. ④ PopupAppItem.CanRunAsAdmin(App.Kind==Win32) 파생 속성. ⑤ GroupPopupWindow: _groupId 보관(편집 시 GetAllAsync 재조회), _editing 가드로 편집 다이얼로그/picker 표시 중 Deactivated 자동닫힘 억제(plan D-5a), OnEditGroupClick은 팝업 XamlRoot로 GroupEditDialog 표시 후 finally에서 _editing 해제+Close. ⑥ 팝업은 독립 프로세스라 App.MainWindow=null → App.xaml.cs 팝업 분기에서 MainWindow=popup 설정해 편집 다이얼로그 아이콘 picker HWND 확보. 빌드 0/0, 테스트 Domain 23 + Application 84(신규 LaunchAsAdmin Packaged 거부 1). 우클릭 메뉴 표시·툴팁·관리자 실행·편집 다이얼로그·picker 동작은 F5 MSIX GUI 수동 검증. (정정: 이 중 "그룹 수정"의 팝업 내 GroupEditDialog 표시·_editing 가드·MainWindow=popup 방식은 ContentDialog가 작은 팝업 XamlRoot에 갇혀 잘리는 문제로, 위 2026-06-03 "메인 앱 위임 방식으로 교체" 항목에서 제거·대체됨. 툴팁·열기·관리자 실행은 유지.)
- 2026-06-03: 하위 폴더 팝업을 호버한 항목 높이에 맞춰 배치 — 기존 PlaceAtAnchor가 자식 팝업 Y를 호버 항목이 아니라 부모 상단(ParentTop − VerticalOverlap)에 정렬해 선택 위치와 무관하게 부모 위쪽에 떴음. y = ParentTop + anchor.ButtonY(부모 클라이언트 기준 항목 Y 오프셋 → 화면 절대 Y 근사)로 변경해 선택한 폴더 위치에 하위 팝업이 표시되도록 함. 미사용이 된 VerticalOverlap 상수 제거. 작업영역 클램프(상하단)는 유지. 1차→2차, 2차→3차 모두 동일 적용. 빌드 0/0.
- 2026-06-03: 폴더 팝업(1차/2차) 헤더와 목록 사이 구분선 추가 — FolderListPopupWindow.xaml(헤더 "폴더"+톱니)·FolderContentsPopupWindow.xaml(헤더 폴더 이름) 루트 Grid에 Auto 행을 하나씩 추가하고 헤더와 목록(ScrollViewer) 사이에 Border(Height=1, DividerStrokeColorDefaultBrush) 배치. 목록은 Grid.Row 1→2로 이동. 빌드 0/0.
- 2026-06-03: 폴더 목록 팝업(FolderListPopupWindow) 2열 이상 그리드 가로 잘림 수정(GUI 검증 완료) — 증상: 2열 이상 설정 시 우측 셀이 팝업 밖으로 나가 잘림. 원인 ① 너비 추정식이 셀 실제 점유 폭(content + 버튼 좌우 패딩)과 바깥 Grid 패딩을 과소 계산. ② 높이는 chrome(AppWindow.Size − ClientSize) 보정이 있었으나 **너비에는 테두리(SetBorderAndTitleBar) 보정이 없어** window 크기로 준 _popupWidth가 그대로 잡혀 client 폭이 그만큼 줄어 콘텐츠가 잘림. 수정: ① 너비 chrome(hChrome) 보정 추가 — windowWidth = _popupWidth + hChrome로 Resize, MoveToTaskbar 배치도 실제 창 폭(_lastAppliedWidth) 사용. ② **추정식 제거 → 측정 기반 자동 너비**: 그리드(ColumnCount≥2)일 때 AdjustToContent에서 Measure(무한 폭)로 콘텐츠 자연 폭을 구해 _popupWidth에 반영(폴더 수가 열 수보다 적으면 실제 폴더 수만큼만, 헤더도 측정 포함이라 최소 폭 자동 보장). 세로 목록(1열)은 360 고정 유지. 빌드 0/0.
- 2026-06-03: 폴더 팝업 닫힘/포커스 메커니즘 재설계(AppGroup 방식 이식, GUI 검증 완료) — 기존 _isActive/마우스 위치(GetCursorPos) 기반 닫힘이 누적·다른 앱 클릭 시 안 닫힘 등 반복 버그를 유발해 전면 교체. AppGroup의 핵심 패턴: ① **자식(2차+) 팝업은 포커스를 뺏지 않고 표시**(AppWindow.Show(activateWindow:false), child.Activate() 제거) → 1차가 활성 상태 유지. ② **1차만 SetForegroundWindow로 포그라운드 포커스 확보**(트레이 클릭 직후 Activate()만으론 부족) → 다른 앱/작업표시줄/바탕화면 클릭 시 1차 Deactivated → CloseSelf → OnClosed가 자식 체인 닫음(전체 닫힘). ③ child.Closed는 ReferenceEquals(_child, child)로 추적만 해제(자식 교체 시 늦게 온 Closed가 새 _child를 null로 덮어쓰던 누적 버그 차단). ④ 폴더 클릭=탐색기 열기+체인 닫기, 하위 탐색은 호버로. ⑤ 마우스 위치 판정(IsPointerOverChain/Window/GetCursorPos)·_isActive·_childOpen 전부 제거(복잡도↓). FolderListPopupWindow/FolderContentsPopupWindow 공통. 빌드 0/0.
- 2026-06-03: 폴더 팝업 후속 수정(GUI 검증 완료) — ① 팝업 가로 폭 확대(1차 FolderListPopupWindow.SingleColumnWidth 360, 2차 FolderContentsPopupWindow.PopupWidth 400). ② 항목 이름 말줄임: 가로 StackPanel은 자식을 무한 너비로 측정해 TextTrimming(CharacterEllipsis)이 동작 안 함 → 항목 버튼 콘텐츠를 Grid(아이콘 Auto + 이름 *)로 변경해 긴 이름이 "..."로 표시. ③ 닫힌 창 접근 COMException(0x800710DD) 근본 차단: 모든 닫기 경로(자가/부모→자식/App→1차 _folderPopup)를 CloseSelf로 통일해 Close() 전 _closed를 동기 set(Closed 이벤트는 큐에서 Tick보다 늦게 와 가드 누설), Content/AppWindow에 접근하는 모든 비동기 진입점(AdjustToContent[SizeChanged], RevealAtTaskbar/RevealAtAnchor[DispatcherQueue], OnHoverTick[타이머])에 _closed 가드 + OnHoverTick COMException try-catch 안전망. ④ 2차→1차 마우스 복귀 시 전체가 닫히던 문제: 창 활성(_isActive)은 마우스 hover로 안 바뀌므로 부적합 → child.Closed 후 GetCursorPos 커서 좌표가 자기 창 사각형 위인지로 판정(복귀 시 유지, 밖이면 체인 종료, _child 교체 시 유지). 빌드 0/0.
- 2026-06-03: 폴더 바로가기(트레이 좌클릭 폴더 팝업) 기능 신규 추가(plan T1~T12) — AppGroup의 "시작 메뉴 폴더" 기능 이식, 기존 "작업 그룹"과 별개. ① Domain: FolderShortcut(Id/Name/Path, Create 검증)·FolderPopupSettings(열/깊이 1~5 클램프·Default(1,2,false)). ② Application: IFolderShortcutRepository·IDirectoryBrowser(DirectoryListing/Status)·IShellOpener. ③ Infrastructure: JsonFolderShortcutRepository(folders.json, JsonGroupRepository 패턴=원자적 쓰기/손상 백업/SemaphoreSlim, Id 단조증가·경로 중복검사)·DirectoryBrowser(숨김 필터/이름 정렬/상태)·ShellOpener(Process.Start ShellExecute)·ShellIcon.OpenForPathAsync(경로 아이콘 public 오버로드 추가, 기존 OpenForAppAsync 무변경)·WorkGroupPaths.FoldersConfigPath. ④ App: FolderPopupSettingsService(LocalSettings, ThemeService 패턴)·FolderIconLoader(경로→ImageSource)·FolderShortcutsViewModel/Item(검색 필터)·TrayMenuPage(placeholder→폴더 관리: 안내/검색/카운트/추가·설정/카드 목록/위치열기·삭제)·FolderEditDialog(이름+FolderPicker, 검증·저장)·FolderListPopupWindow(좌클릭 1차 목록, ColumnCount 1=세로/2~5=그리드, 톱니→트레이메뉴 탭)·FolderContentsPopupWindow(2차 파일/하위폴더, SubfolderDepth 재귀, 부모 좌/우 배치)·FolderPopupSettingsDialog. ⑤ 트레이 동작 변경: 좌클릭=폴더 팝업(TrayIconService.LeftClickRequested, WM_LBUTTONUP만·DBLCLK 상수 제거), 메인 창은 우클릭 메뉴 "열기"만(OpenRequested 유지). App.ShowFolderListPopup/ShowTrayMenuFromPopup, MainShell.SelectTrayMenu. ⑥ B2 포커스 가드: 부모-자식 팝업 체인(_childOpen/_isActive/_child/CloseChainRequested)으로 2차 팝업 표시 중 1차 유지, 파일 실행·최대깊이 폴더 열기 시 체인 전체 닫기, ChildPopupAnchor로 배치. ⑦ ServiceConfiguration DI 등록(저장소/브라우저/오프너/설정서비스/뷰모델). 빌드 0/0, 테스트 106/106(Domain 23: 신규 FolderShortcut 4+FolderPopupSettings 4, Application 83: 신규 JsonFolderShortcutRepository 8+DirectoryBrowser 4). DI 런타임 해석·트레이 좌/우클릭·팝업 표시/위치/호버/2차/포커스 체인·설정 반영은 F5 MSIX GUI 수동 검증.
- 2026-06-03: 트레이 아이콘을 앱 아이콘(AppIcon.ico)으로 적용 — TrayIconService.Initialize에서 LoadIcon(IDI_APPLICATION) 기본 아이콘 대신 LoadTrayIcon()으로 Assets\AppIcon.ico를 LoadImage(IMAGE_ICON, 작은 아이콘 크기 SM_CXSMICON/CYSMICON, LR_LOADFROMFILE) 로드. 실패 시 시스템 기본 아이콘 폴백. 파일에서 로드한 핸들은 소유(_ownedIcon)로 Dispose에서 DestroyIcon 해제. 경로는 AppContext.BaseDirectory 기준. 빌드 0/0. (트레이 표시는 F5 GUI 수동 검증.)
- 2026-06-03: 핀 팝업 항목 마우스 오버 배경 숨김 — GridView.Resources에서 GridViewItemBackgroundPointerOver/Pressed를 Transparent로 재정의해 항목 hover/누름 시 기본 강조 배경이 보이지 않게 함(아이콘 호버 확대 애니메이션만 유지). 빌드 0/0.
- 2026-06-03: 핀 팝업 표시 시 크기 점프·초기 깜빡임 제거 — 원인: 생성자에서 InitialPopupHeight(200)·정위치로 배치 후 Activate로 즉시 표시 → 비동기 LoadAsync/AdjustToContent가 실제 높이로 리사이즈하는 과정이 화면에 보임(점프), 콘텐츠/아이콘 로드 전 빈 창이 먼저 보임(깜빡임). 수정: ① 생성자에서 클릭 시점 커서/모니터(_metrics) 1회 캡처 후 창을 화면 밖(OffScreen -32000)으로 이동 — 측정/리사이즈를 화면 밖에서 끝냄. ② LoadAsync에서 아이콘(LoadIconAsync)까지 Task.WhenAll로 대기(실패 무시). ③ finally에서 RevealAtTaskbar로 최종 측정→정위치 이동(_positioned=true)해 처음으로 표시. PositionNearTaskbar(Resize+Move)를 AdjustToContent의 Resize와 MoveToTaskbar(Move, _metrics 사용)로 분리, 표시 전에는 Move 생략. 빌드 0/0. (점프·깜빡임 제거는 F5 GUI 수동 검증.)
- 2026-06-03: 핀 팝업 아이콘 호버 확대 애니메이션 — GridView ItemTemplate StackPanel에 중앙 기준 ScaleTransform + PointerEntered/Exited 이벤트. OnIconPointerEntered/Exited에서 AnimateIconScale(Storyboard, ScaleX/Y DoubleAnimation, 120ms CubicEase EaseOut)로 1.0↔1.15 부드럽게 전환. 빌드 0/0. (호버 효과는 F5 GUI 수동 검증.)
- 2026-06-03: 핀 클릭 시 팝업 창이 작업 표시줄/Alt+Tab에 노출되던 것 차단 — GroupPopupWindow.ConfigurePresenter에서 AppWindow.IsShownInSwitchers=false 설정. 핀 클릭으로 뜬 팝업 프로세스 창이 작업 표시줄 버튼/스위처에 별도 앱 아이콘·미리보기로 보이던 문제 제거(IsShownInSwitchers는 OverlappedPresenter가 아니라 AppWindow 속성). 빌드 0/0. (노출 차단은 F5 GUI 수동 검증.)
- 2026-06-03: 그룹 추가/수정 화면 개선(plan T1~T5) — ① 선택 앱 목록 위 왼쪽에 앱 개수 표시("앱 N개", GroupEditViewModel.AppCountText, SelectedApps.CollectionChanged 생성자 1회 구독으로 갱신). ② "앱 추가" 버튼을 "+" 아이콘만(SymbolIcon Add + ToolTip "앱 추가"). ③ 앱 추가 버튼 왼쪽에 "팝업 이름 표시" 토글(ToggleSwitch) — **그룹별 설정**. AppGroup에 ShowPopupHeader 속성 추가(Create/Restore 선택적 매개변수 기본 true, SetShowPopupHeader), JsonGroupRepository.GroupDto에 bool? ShowPopupHeader 추가(키 고정·레거시 JSON은 ?? true 마이그레이션·SchemaVersion 불변), GroupPopupWindow.LoadAsync가 group.ShowPopupHeader로 헤더 TitleText 표시/숨김(에러 메시지는 항상 표시, RowSpacing 제거+TitleText Margin으로 헤더 숨김 시 잔여 여백 없음). ④ 선택 앱 목록 항목을 Border로 감싸 하단 1px 구분선(DividerStrokeColorDefaultBrush)으로 행 구분. 도메인 호출자(테스트 다수)는 default라 무변경. 빌드 0/0, 테스트 86/86(Domain 15 신규4 + Application 71 신규2). (토글 저장→팝업 반영·카운트 갱신·구분선·헤더 숨김 여백은 F5 GUI 수동 검증.)
- 2026-06-03: 핀 그룹 팝업 아이콘 아래 앱 이름 제거 — DataTemplate의 이름 TextBlock(DisplayName) 삭제, 아이콘 타일만 표시. 콘텐츠 높이 자동 맞춤으로 팝업 세로도 함께 축소. PopupAppItem.DisplayName 속성은 유지. 빌드 0/0.
- 2026-06-03: 핀 그룹 팝업 흰 테두리 → 메인 창 스타일로 재구성 — borderless(SetBorderAndTitleBar(false,false)) 창에 Windows가 그리는 흰 윤곽선은 DWM(COLOR_NONE)으로도 제거 불가라, 메인 창과 동일한 표준 창 프레임 방식으로 전환. ① SetBorderAndTitleBar(true,false): 둥근 모서리/시스템 테두리는 유지하고 타이틀바(캡션 버튼)만 숨김. ② SystemBackdrop=MicaBackdrop()(메인 창과 동일 재질). ③ XAML 외곽 Border(아크릴 배경·CornerRadius·테두리)를 제거하고 루트를 Grid로 — 배경/모서리는 창 프레임이 담당. 앞선 DWM P/Invoke(상수·DllImport·RemoveSystemBorder)·관련 using 제거. 세로 높이 콘텐츠 맞춤(AdjustToContent)은 유지하되, 표준 프레임 전환으로 비클라이언트(테두리/캡션) 영역이 생겨 콘텐츠가 잘리던 것을 보정 — 측정한 클라이언트 높이에 chrome(AppWindow.Size.Height − ClientSize.Height)을 더해 창 높이를 정한다. 또한 ExtendsContentIntoTitleBar=true로 콘텐츠를 타이틀바 영역까지 확장해 상단 빈 캡션 여백 제거(타이틀바는 SetBorderAndTitleBar(true,false)로 숨김). 추가로 GridView가 내부 세로 스크롤(가상화)로 필요 높이를 줄여 보고해 콘텐츠 측정이 부정확→세로 잘림 발생하던 문제를, GridView ScrollViewer.VerticalScrollMode/VerticalScrollBarVisibility=Disabled로 전체 콘텐츠를 펼쳐 측정 정확화. 그래도 DispatcherQueue 1회 측정은 GridView 아이템 컨테이너 생성 전에 실행돼(콘텐츠 높이 과소→창 고정→라벨 잘림) 부정확 → 루트 SizeChanged를 구독해 컨테이너 생성·배치로 콘텐츠가 커진 시점에 재측정·재조정하고, _lastAppliedHeight 가드로 동일 높이 재적용(무한 루프) 차단. 빌드 0/0. (외곽 표시·잘림·여백은 GUI 수동 검증.)
- 2026-06-03: 핀 그룹 팝업(GroupPopupWindow) 테두리 제거 + 세로 높이 콘텐츠 맞춤 — ① 루트 Border의 BorderBrush/BorderThickness("1") 제거(둥근 모서리 CornerRadius 8·배경은 유지)로 회색 테두리선 삭제. ② 고정 높이 PopupHeight(440)를 폐기하고 InitialPopupHeight(200)로 초기 배치 후, 아이템 로드 완료 시(LoadAsync finally→DispatcherQueue) AdjustToContent에서 루트 콘텐츠를 측정(UpdateLayout+Measure, 너비 고정·높이 무한 제약)해 DesiredSize×RasterizationScale로 창 높이를 줄이고 작업 표시줄 근처로 재배치. PositionNearTaskbar(int height)로 시그니처 변경. 아이콘 수에 맞춰 세로 여백 제거. 빌드 0/0. (실제 팝업 크기·표시는 GUI 수동 검증.)
- 2026-06-03: 앱 아이콘(AppIcon.ico) 일괄 적용 — ① 실행 파일(.exe) 아이콘(csproj `ApplicationIcon`) ② 메인 창 작업 표시줄/Alt+Tab 미리보기 아이콘(App.xaml.cs `AppWindow.SetIcon(@"Assets\AppIcon.ico")`, 창 생성 블록 내 1회) ③ 타이틀바 아이콘을 AppIcon.ico로 변경 + 28px(IconSource는 16px 고정이라 `TitleBar.LeftHeader`에 Image 배치, 제목 왼쪽) ④ 정보 화면 앱 이름 카드 아이콘 AppIcon.ico + 48px(`SettingsCard.HeaderIcon`). AppIcon.ico를 루트→`Assets/`로 복사 후 csproj `Content` 포함(SetIcon/ms-appx 참조용). 기존 Square44x44Logo 타일 자산은 미변경(타일/스토어 로고 유지). 빌드 0/0, 테스트 80/80. (아이콘 시각 표시는 F5 MSIX GUI 수동 검증.)
- 2026-06-03: 그룹 수정 화면 이름 클릭-편집 + 핀 재등록 경고 — 수정 모드에서 그룹 이름을 읽기전용으로 표시하다 클릭하면 입력창(TextBox)으로 전환(GroupEditViewModel.IsNameEditing/NameDisplayVisibility/NameEditVisibility, code-behind에서 DispatcherQueue로 포커스+SelectAll). 이름이 원래와 달라지면(ShowRenameWarning) 입력 영역 아래 경고 InfoBar 표시("작업 표시줄에 핀한 기존 항목을 제거하고 다시 핀해야 합니다"). 핀 표시 이름은 Windows 제약상 프로그래밍 방식 자동 변경 불가라 안내만 함. 신규 추가는 기존대로 즉시 입력·경고 없음. GroupEditDialog.xaml Grid 4→5행 재구성. 빌드 0/0, 테스트 80/80. (이름 전환·포커스 등 UI 동작은 GUI 수동 검증.)
- 2026-06-02: 드래그 비주얼 빈 이미지 수정 — 화면 Image에 쓰이는 라이브 BitmapImage를 SetContentFromBitmapImage에 넘기면 드래그 표면에 빈 이미지로 렌더되는 문제. 아이콘 파일(PNG 우선, 없으면 .ico)을 SoftwareBitmap(BGRA8)으로 새로 로드해 SetContentFromSoftwareBitmap으로 지정(DragStarting deferral로 비동기 로드 대기). 드래그 비주얼 크기는 최대 128px(종횡비 보존·업스케일 금지). 드래그/핀 동작 정상, GUI 검증 완료. 빌드 0/0, 테스트 80/80.
- 2026-06-02: 그룹 목록 드래그 비주얼=그룹 아이콘 — 작업 표시줄로 드래그 시 커서 이미지가 항목 카드 스냅샷이 아니라 그룹 아이콘으로 표시. ListView CanDragItems/DragItemsStarting → 카드 Border CanDrag="True"+DragStarting로 전환, DragStartingEventArgs.DragUI.SetContentFromBitmapImage(item.Icon) 사용. 기존 .lnk 데이터 공급자(임시복사+SetDataProvider deferral) 동일 이식. DragStarting엔 Cancel이 없어 미저장 그룹은 데이터 미설정(no-op)+안내. 핀 드롭 페이로드 불변. 빌드 0/0, 테스트 80/80. (드래그/드롭/핀 실제 동작은 GUI 수동 검증.)
- 2026-06-02: 데이터 저장을 그룹별 폴더 구조로 변경 — `WorkGroup\Groups\{groupId}\`(아이콘=`Icons\{id}.ico/.png`, 바로가기=`{이름}.lnk`). 그룹 삭제 시 폴더째 제거, 고아 정리도 폴더 단위. 폴더 키=그룹 id(GUID), 마이그레이션 없음(기존 그룹은 재저장 시 새 구조 생성·기존 flat 파일/핀은 수동 정리). WorkGroupPaths(GroupsDirectory/GroupIconsDirectory, 옛 Icons/Shortcuts 제거)·ShortcutService(그룹 폴더 .lnk)·GroupAppService(폴더 단위 저장/삭제/고아정리)·GroupIconLoader·DI 변경. 인터페이스/직렬화 불변. 빌드 0/0, 테스트 80/80.
- 2026-06-02: 그룹 아이콘 .ico+.png 동시 생성, 목록은 .png로 표시(선명도 근본 해결) — IconService가 {id}.ico(작업표시줄)와 함께 {id}.png(원본 해상도, EncodePngAsync no-scale)를 저장(CreateGroupIconAsync/CreateDefaultAsync). GroupIconLoader.GetPngPath 추가. GroupListItem은 PNG 우선 로드(없으면 .ico 폴백, 둘 다 IgnoreImageCache)→ .ico 프레임 디코드 없이 GPU 축소로 선명. GroupAppService 삭제/고아정리에 .png 포함. IconServiceTests에 PNG 생성 검증 추가. 기존 그룹은 재저장 시 .png 생성됨. 빌드 0/0, 테스트 80/80.
- 2026-06-02: 그룹 목록 .ico 표시 방식 수정(선명도) — DecodePixelWidth=32가 .ico의 작은 프레임 선택/업스케일을 유발해 흐림. 크기 미지정 디코드(최대 프레임)→Image(32px) GPU 축소로 변경(편집 미리보기와 동일 방식). IconService는 .ico 프레임을 큰 것부터 기록(frame0=최대). 참고: 기존 그룹 .ico는 옛 파이프라인 산출물이라 그룹 수정→확인으로 재저장해야 개선된 .ico로 재생성됨(수동, 사용자 결정). 빌드 0/0, 테스트 80/80.
- 2026-06-02: 작업 그룹 목록 아이콘을 다시 .ico로 표시 — .ico 생성 선명도 개선(업스케일 금지+종횡비 보존) 후, 목록도 모든 그룹을 생성된 .ico로 통일(앞서 넣은 CustomImage 원본 직접 로드 분기 제거). GroupListItem.TryLoadCustomImage 제거. .ico 로드 시 DecodePixelType.Logical + IgnoreImageCache는 유지(선명/즉시반영). 빌드 0/0.
- 2026-06-02: .ico 생성 선명도 개선 — 기존엔 모든 프레임을 size×size로 stretch해 ① 작은 원본(~100px)을 256으로 업스케일(흐림) ② 비정사각 원본 왜곡. 변경: 원본 최대 변 이하의 표준 크기(16/24/32/48/64/128/256)만 생성 + 최상위는 원본 크기(업스케일 금지), 각 프레임은 종횡비 보존 축소(Fant) 후 정사각 투명 캔버스 중앙 배치. IconService.WriteIcoAsync/EncodeFrameAsync/ScaleAsync 추가. 프레임 수 가변이라 IconServiceTests의 프레임 수 단정(4)→(≥1)로 완화. 빌드 0/0, 테스트 80/80(.ico 생성·디코드 테스트 포함).
- 2026-06-02: 작업 그룹 목록 그룹 아이콘 선명도 — 목록이 생성된 .ico(32/48 프레임, 미리 축소돼 흐림)를 쓰던 것을, CustomImage(리소스/사용자 이미지) 그룹은 원본을 네이티브 해상도로 직접 로드(편집 미리보기와 동일). GroupListItem.TryLoadCustomImage 추가(ms-appx 절대 URI/사용자 파일, IgnoreImageCache). BuiltIn/MemberApp은 기존 .ico 유지. 빌드 0/0.
- 2026-06-02: 작업 그룹 목록 그룹 아이콘 3종 버그 수정 — ① 수정 후 미반영: 같은 .ico 경로 재로드 시 WinUI 이미지 캐시가 옛 아이콘 반환 → `CreateOptions=IgnoreImageCache`. ② 잘림: Border(44/Padding4/테두리1) 내부 영역(≈34)보다 Image(36)가 커서 클리핑 → Image 36→32(Stretch=Uniform). ③ 흐림: DecodePixelWidth=32 디코드 후 확대 → `DecodePixelType=Logical`(+DecodePixelHeight)로 DPI 기준 디코드(.ico 48/256 프레임 활용). GroupListItem.cs/WorkGroupsPage.xaml. 빌드 0/0.
- 2026-06-02: 작업 그룹 헤더-목록 간격 축소 — InfoBar가 별도 행이라 RowSpacing(12)이 이중 적용되던 문제. InfoBar를 콘텐츠 그리드 안으로 이동하고 외곽 행을 3→2개로 축소, 메시지 없을 때 InfoBar를 접도록 StatusVisibility 추가(WorkGroupsViewModel). 헤더→목록 간격 ≈24→12px. 빌드 0/0.
- 2026-06-02: 그룹 삭제 시 상단 안내 메시지 제거 — WorkGroupsViewModel.DeleteAsync의 StatusMessage 설정 삭제(InfoBar는 다른 메시지에 계속 사용). 빌드 0/0.
- 2026-06-02: 작업 그룹 헤더 개편 — "그룹 추가" 버튼을 설명글 아래 줄로 이동하고, 같은 줄 왼쪽에 등록 그룹 수 표시(예: "3 그룹"). WorkGroupsViewModel.GroupCountText 추가(LoadAsync에서 OnPropertyChanged 통지), WorkGroupsPage.xaml 헤더를 제목/부제 + [개수·추가] 2단 구성으로 변경. 빌드 0/0.
- 2026-06-02: 작업 그룹 목록 멤버 앱 아이콘에 앱 이름 툴팁 — 멤버 아이콘 타일에 `ToolTipService.ToolTip="{x:Bind DisplayName}"` 바인딩(마우스 오버 시 앱 이름). WorkGroupsPage.xaml만 변경. 빌드 0/0.
- 2026-06-02: 작업 그룹 목록 아이콘에 사각 테두리 — 그룹 아이콘 Border에 테두리선(CardStrokeColorDefaultBrush, 1px) 추가, 멤버 앱 미니 아이콘을 둥근 사각 타일(30×30, CornerRadius 6, 보조 배경+테두리)로 감쌈. WorkGroupsPage.xaml만 변경(바인딩/로직 불변). 빌드 0/0.
- 2026-06-02: "앱 추가" 팝업 선택 체크 + 토글 — 추가된 앱을 팝업에서 제외하던 동작을 변경해, 목록에 남기고 항목 앞에 체크 아이콘 표시(LaunchTarget 기준). 항목 클릭 시 추가↔해제 토글(ToggleApp). PopupAppItem.IsSelected/SelectionGlyphVisibility 추가, RefreshAvailable 전체표시+IsSelected 계산, 픽커 DataTemplate에 고정폭 체크 열, OnAppPickerItemClick→ToggleApp. 본문 선택 목록/삭제는 현행 유지. 공개 API/도메인 불변. 빌드 0/0, 테스트 80/80.
- 2026-06-02: Win32 앱 아이콘도 셸 렌더로 전환 — 설치 목록의 Win32 .lnk(콘솔/스크립트: Strawberry Perl CPAN 도구, VS 개발자 명령 프롬프트 등) 아이콘 누락 해결. `GetThumbnailAsync`(.lnk에서 자주 실패) 대신 `IShellItemImageFactory`를 파일 경로에도 적용(DevDashboard 동일, 탐색기/시작 메뉴 아이콘). `PackagedAppIcon`→`ShellIcon` 일반화: `OpenForAppAsync(AppEntry,size)`가 packaged=shell:AppsFolder\AUMID, Win32=파일 경로로 추출. 크기 캐스케이드(256/128/64/48/32)+플래그 폴백(ICONONLY→일반) 추가. IconService/AppIconLoader는 모든 Kind에 셸 아이콘 우선, 실패 시 기존 폴백. 빌드 0/0, 테스트 80/80. (시각은 GUI 수동 검증.)
- 2026-06-02: 패키지 앱 아이콘 셸 렌더 전환 — `GetLogo`는 앱별 매니페스트 로고 여백 편차로 아이콘 크기가 들쭉날쭉(targetsize-unplated 에셋만 둔 앱은 작게 보임). `PackagedAppIcon.OpenIconStreamAsync`를 `shell:AppsFolder\{AUMID}` + `IShellItemImageFactory.GetImage`(SIIGBF_BIGGERSIZEOK|ICONONLY)로 재작성 → 시작 메뉴와 동일한 균일 아이콘. HBITMAP(DIBSection)→`SoftwareBitmap`(BGRA8 premultiplied)→PNG 스트림(System.Drawing 미사용, CsWin32 P/Invoke). NativeMethods.txt에 SHCreateItemFromParsingName/IShellItemImageFactory/GetObject/DIBSECTION/DeleteObject 추가. 호출처(IconService/AppIconLoader) 메서드명만 갱신, 분기·폴백 로직 불변. 새 NuGet 의존성 없음. 빌드 0/0, 테스트 80/80. (셸 아이콘 시각 정합은 GUI 수동 검증.)
- 2026-06-02: 패키지 앱 아이콘 추출 개선(GetLogo) — UWP/Store 앱(`package.Logo` 경로 없음)의 아이콘 누락 해결. 신규 `PackagedAppIcon.OpenLogoStreamAsync`(AUMID→`PackageManager`→`AppListEntry.DisplayInfo.GetLogo`→`IRandomAccessStream`, OS 19041 가드, 실패 null). `IconService.ResolveMemberBitmapAsync`(그룹 .ico)·`AppIconLoader.LoadAsync`(팝업/미니 아이콘) 양쪽에 Packaged 분기로 GetLogo 우선 적용, 실패 시 기존 폴백 유지. 스트림 디코드 `DecodeStreamAsync` 공통화. WinRT 네이티브(새 의존성 없음), 공개 API/직렬화 무변경. 빌드 0/0, 테스트 80/80.
- 2026-06-02: 그룹 편집 다이얼로그 성능 버그 수정(근본 원인) — InitializeAsync가 오픈 시 설치앱 전체 인벤토리 + 수백 개 셸 썸네일 아이콘을 eager 로드해 UI 스레드 포화(멤버 목록 지연 + 취소 지연). 편집 멤버는 group.Apps에서 즉시 복원(인벤토리 무관), 설치앱 인벤토리는 "앱 추가" Flyout 열 때, 리소스 아이콘 그리드는 아이콘 Flyout 열 때 지연 로드(1회 가드 + ProgressRing). 빌드 0/0, 테스트 80/80. (근본 원인·가설은 plan.md Debug 섹션.)
- 2026-06-02: 그룹 추가/수정 다이얼로그 전면 개편 — 상단 [아이콘 미리보기][이름(15자)], 아이콘 클릭 시 사용자 이미지/리소스 아이콘 선택(단일 Flyout 토글 + GridView), "앱 추가" Flyout로 설치 앱을 선택 목록에 추가(항목별 삭제), 확인 시 빈 목록·이름 중복 검증(GetAllAsync 스냅샷). 기존 ComboBox/체크박스 모델 제거(SelectableAppItem 삭제, PopupAppItem 재사용). 리소스 아이콘 91개 번들(Assets/GroupIcons) + IconService ms-appx 디코드 + ResourceIconCatalog. 도메인/직렬화 무변경(리소스=CustomImage ms-appx URI). 빌드 0/0, 테스트 80/80.
- 2026-06-02: 작업 그룹 목록을 항목별 개별 카드로 표시(SettingsCard 목록과 동일한 룩) — 단일 ListView 카드 래퍼 제거, 각 GroupListItem을 둥근 카드 Border(간격 8)로, ListViewItem 패딩 제거(전체 폭). 드래그 핀/수정·삭제/바인딩 계약 불변. 빌드 0/0, 테스트 80/80.
- 2026-06-02: WinUI Gallery Fluent 디자인 정합(참조: NapCat) — CommunityToolkit.WinUI.Controls.SettingsControls 8.2.251219 추가(WinAppSDK 1.8 호환). 디자인 토큰 리소스(Resources/Spacing.xaml=PageContentPadding/ContentMaxWidth/SideNavWidth, ControlStyles.xaml=CardStyle/Hero/Primary·SecondaryActionStyle/SettingsGroupHeaderStyle). 셸: 커스텀 TitleBar(ExtendsContentIntoTitleBar) + NavigationView 정교화(280 pane, 컨텐츠 보더 제거, Transparent). 설정/정보 페이지를 SettingsCard로, 작업그룹/트레이/정보/설정 전 페이지를 공통 레이아웃(ScrollViewer/PageContentPadding/MaxWidth/헤더)으로 통일. 기능/바인딩 불변(코드비하인드 미변경). 빌드 0/0, 테스트 80/80. (TitleBar 드래그/캡션 시각은 GUI 수동 확인 대상.)
- 2026-06-02: WinUIEx 2.9.1 재도입 — 메인 창을 WindowEx로 전환해 창 크기/위치 지속(PersistenceId="WorkGroupMain")·최소 크기(800×560) 관리. Mica는 표준 SystemBackdrop 유지(WinUIEx 자체 Backdrop은 CS0618 deprecated). 트레이 종료 시 `_window.Close()`로 persistence 저장 보장(닫기→트레이 숨김 구조에서 Window.Closed 발생 시점 확보). 트레이는 WinUIEx 미지원이라 Win32 Shell_NotifyIcon 유지. 빌드 0/0, 테스트 80/80.
- 2026-06-02: UI 전면 개편(Plan 3, T1~T8) — NavigationView 셸(MainShell: 작업 그룹/트레이 메뉴/설정/정보) + Fluent(Mica 백드롭). 설정 페이지(자동 시작 토글 + 테마 시스템/다크/라이트, ThemeService LocalSettings 영속). 정보 페이지(버전 + 오픈소스 라이선스 목록). 트레이 메뉴 placeholder. 그룹 추가/수정은 ContentDialog(설치앱 체크박스 선택 + 아이콘 설정 + 미리보기). 작업 그룹 페이지(그룹 아이콘 + 2라인[이름/멤버 아이콘] + 수정/삭제 아이콘 버튼 + 드래그 핀 + 삭제 확인). 단일 MainPage/MainViewModel 제거. 빌드 0/0, 테스트 80/80.
- 2026-06-02: T12 — 자동 시작(StartupTask + 토글) + Win32 트레이 아이콘(Shell_NotifyIcon, 열기/종료) + 실행 별칭 `WorkGroupSpike.exe`→`WorkGroup.exe` finalize + README/notes 갱신. 빌드 0/0.
- 2026-06-02: T11 — 정식 팝업 런처(GroupPopupWindow, 아이콘 그리드 + 클릭 실행) + IAppLauncher + AppIconLoader. spike 팝업 제거.
- 2026-06-02: T9+T10 — 메인 관리 화면(MainViewModel/MainPage) + 그룹 드래그 등록(검증된 패턴) + DI 조립(ServiceConfiguration/WorkGroupPaths).
- 2026-06-01: T2 배포 수정 — 앱을 MSIX 패키지 모드로 전환(`WindowsPackageType=None` 제거). VS F5 배포 시 DEP1700(.appxrecipe 없음) 해결. 빌드 0/0, .appxrecipe 생성 확인.
- 2026-06-01: T1b — 레이어 프로젝트(Domain/Application/Infrastructure) + 테스트 2개(xUnit) 생성, 프로젝트 참조 연결, 솔루션 등록. AGENTS.md/README.md/notes.md 작성. 전체 빌드 0/0, 테스트 2/2 통과.
- 2026-06-01: T1a — WinUI3 앱(`WorkGroup.App`, net10.0-windows10.0.19041.0) + `WorkGroup.slnx` 스캐폴딩. `dotnet build WorkGroup.slnx` 0/0 검증. git 저장소 초기화.
